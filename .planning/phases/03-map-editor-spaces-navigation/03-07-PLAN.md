---
phase: 03-map-editor-spaces-navigation
plan: 07
type: execute
wave: 6
depends_on: ["03-06"]
files_modified:
  - src/features/person-map/editor/PersonPicker.tsx
  - src/features/person-map/editor/PersonPicker.module.css
  - src/features/profile/ProfileSidebar.tsx
  - src/features/profile/ProfileSidebar.module.css
  - src/app/App.tsx
  - src/features/person-map/MapView.tsx
  - tests/features/personPicker.test.ts
  - tests/features/appearsOn.test.ts
  - e2e/place-person.spec.ts
autonomous: true
requirements: [MAP-05]
must_haves:
  truths:
    - "While viewing a map, a user can open a searchable person picker (the Person tool), pick an existing person, and drop a marker on the active map (D-11)"
    - "Placing an already-placed person on a second map produces a second Marker row for the same personId — identity (photo/name/data) stays a single canonical Person record (D-13)"
    - "A person's profile shows an 'Appears on:' list of every map they are placed on (D-12)"
    - "Each 'Appears on' row is a jump-to-placement button: it sets that map active and centers/selects the placement"
    - "Editing a person (name/photo) is reflected on every placement across all maps because identity is shared (success criterion 4)"
    - "A person placed on no map shows the muted 'Not placed on any map yet.' state (D-12)"
  artifacts:
    - path: "src/features/person-map/editor/PersonPicker.tsx"
      provides: "searchable list of existing people; on pick calls upsertMarker({kind:'person', mapId, personId, x, y, layerId}) — a new Marker row (D-11)"
      contains: "upsertMarker"
    - path: "src/features/profile/ProfileSidebar.tsx"
      provides: "the 'Appears on:' section: db.markers.where('personId').equals(id) grouped by mapId, each row a jump-to-placement button (D-12)"
      contains: "Appears on"
    - path: "src/app/App.tsx"
      provides: "wires the picker into the Person tool drop and the 'Appears on' jump (sets activeMapId + selects/centers the marker)"
    - path: "tests/features/personPicker.test.ts"
      provides: "unit test: pick = new Marker row for the personId; second placement = two markers, one Person (MAP-05)"
      contains: "upsertMarker"
    - path: "tests/features/appearsOn.test.ts"
      provides: "unit test: 'Appears on' query groups a person's markers by mapId; canonical-record edit propagates to every placement (criterion 4)"
      contains: "personId"
  key_links:
    - from: "src/features/person-map/editor/PersonPicker.tsx"
      to: "src/db/repository.ts"
      via: "on pick: upsertMarker({kind:'person', mapId, personId, x, y, layerId}) — new row, no id (D-11/D-13)"
      pattern: "upsertMarker"
    - from: "src/features/profile/ProfileSidebar.tsx"
      to: "src/db/schema.ts"
      via: "Appears on reads db.markers.where('personId').equals(entity.id), grouped by mapId (D-12)"
      pattern: "markers.where\\('personId'\\)"
    - from: "src/features/profile/ProfileSidebar.tsx"
      to: "src/app/App.tsx"
      via: "an Appears-on row click sets the target map active and centers/selects that placement"
      pattern: "(onShowOnMap|setActiveMapId|onJumpToPlacement)"
---

<objective>
Deliver the MAP-05 user-facing placement flow as a vertical slice: the map-side searchable PersonPicker wired to the Person tool (D-11), and the profile "Appears on:" list with jump-to-placement (D-12) — both riding the canonical-Person / N-Marker-rows model proven at the data layer in 03-01 (RESEARCH Pattern 4). After this plan a user can place an existing person on a map from the editor, place the same person on multiple maps, see every map a person appears on from their profile, and confirm that editing a person propagates to every placement — success criterion 4, which was not user-deliverable before.

Purpose: 03-01 added the multi-placement data foundation (markers indexed by `mapId`+`personId`, two rows for one `personId`) and 03-04 made markers selectable/transformable, but no plan wired the user-facing entry points. The Person tool sits in the 03-03 ToolPalette with no picker behind it (only the Portal tool got its PortalTargetPicker in 03-06); this plan closes that gap. MAP-05 is the only phase requirement without a covering user-facing plan.
Output: `PersonPicker` (analog `browse/BrowseList.tsx`), the extended `ProfileSidebar` "Appears on" section (extends the existing Phase-1/2 sidebar), the App/MapView wiring for the Person-tool drop and the jump-to-placement, and unit + E2E tests proving placement and canonical-record propagation.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-map-editor-spaces-navigation/03-CONTEXT.md
@.planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md
@.planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md
@.planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md
@.planning/phases/03-map-editor-spaces-navigation/03-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/features/person-map/editor/PersonPicker.tsx` (+ `.module.css`) — a paper searchable list of existing people (avatar thumb + name), opened by arming the Person tool / a "place person" control (UI-SPEC S16). On pick it calls `upsertMarker({ kind:'person', mapId: activeMap.id, personId: picked.id, x, y, layerId: activeLayerId })` — a NEW Marker row (no `id`), so placing an already-placed person produces a second placement (multi-placement, D-13). Empty state: muted "No people yet. Create one with + New." (UI-SPEC S16). Live-filtered search. Analog: `browse/BrowseList.tsx` (searchable/scrollable entity list) + `nav/NewEntityMenu.tsx` (Radix surface).
- `src/features/profile/ProfileSidebar.tsx` (MODIFIED) — adds an "Appears on:" section (UI-SPEC S4) for People profiles: an `APPEARS ON` eyebrow (13px/600 uppercase) + a list built from `db.markers.where('personId').equals(entity.id).toArray()` grouped by `mapId`. Each row is a `<button>` → calls a jump-to-placement callback that sets that map active and selects/centers the placement (extends Phase-2 D-16 "show on map"). Empty: muted "Not placed on any map yet." Renders map names as React children only — XSS-safe (T-03-01). Lives only on the People path (`type === 'people'`).
- `src/app/App.tsx` (MODIFIED) — wires (a) the Person-tool drop to open the PersonPicker and place on the active map at the drop point, and (b) the "Appears on" jump: `onJumpToPlacement(mapId, markerId)` sets `activeMapId` (from 03-02) and passes the target marker id to MapView to select + center it (reuses the existing `showOnMap` plumbing l.167-172).
- `src/features/person-map/MapView.tsx` (MODIFIED) — the Person tool (already in the ToolPalette from 03-03) opens the PersonPicker on arm/drop; on pick, places the marker at the chosen point via `upsertMarker` (one-shot, returns to Select); accepts a "center/select this marker id" input so the jump-to-placement focuses the placement.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonPicker (searchable, places a new Marker row) + wire the Person tool drop in MapView/App + placement unit test</name>
  <files>src/features/person-map/editor/PersonPicker.tsx, src/features/person-map/editor/PersonPicker.module.css, src/features/person-map/MapView.tsx, src/app/App.tsx, tests/features/personPicker.test.ts</files>
  <read_first>
    - src/features/browse/BrowseList.tsx (the searchable/scrollable entity list analog — row structure, useLiveQuery(db.people), empty/loading states, windowing, "render text as React children" XSS rule)
    - src/features/nav/NewEntityMenu.tsx (the Radix surface analog for the popover; create-person entry for the empty state)
    - src/features/person-map/AvatarMarker.tsx (the avatar-thumb + initials fallback rendering to mirror in the picker rows)
    - src/db/repository.ts (upsertMarker as extended in 03-01 + layerId in 03-05 — kind/personId/x/y/layerId; the no-id => new row contract; the validate->stamp->emit path l.1-7)
    - src/features/person-map/MapView.tsx (as modified by 03-05/03-06 — the tool-mode wiring, the Person tool in the palette, the active-layer + activeLayerId state, the one-shot return-to-Select pattern used by the Portal tool in 03-06)
    - src/app/App.tsx (activeMapId/setActiveMapId from 03-02 l.33; the handleSaved auto-place block l.139-150 which already demonstrates the upsertMarker center-of-map placement; the showOnMap plumbing l.167-172)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 4 "place existing person = new Marker row" l.322-331; the upsertMarker code l.330)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (S16 person picker l.225-228; the place-person flow + empty/searching states; copy "Place person" l.264)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (PersonPicker via BrowseList analog l.242-245; reactive reads via useLiveQuery l.283-285)
  </read_first>
  <behavior>
    - Picking a person from the picker calls upsertMarker with kind 'person', the active map id, the picked person id, the drop x/y (image-space), and the active layer id, with NO id — producing a NEW Marker row.
    - Placing the SAME person on a SECOND map produces a SECOND Marker row: two markers for one personId, exactly one Person record (MAP-05, criterion 4).
    - The picker filters the people list live by the search query (case-insensitive name match).
    - With zero people, the picker shows the empty state rather than an empty list (no crash).
  </behavior>
  <action>
    Create `src/features/person-map/editor/PersonPicker.tsx` (+ `.module.css`): a Radix Popover or Dialog (prefer the already-installed `@radix-ui/react-dialog` per RESEARCH — Popover is optional/not installed) titled per UI-SPEC, hosting a searchable list of existing people via `useLiveQuery(() => db.people.orderBy('name').toArray())` (mirror BrowseList's reactive read + windowing approach; for v1 a simple scroll list is acceptable since people lists are modest). Each row shows the avatar thumb (or initials fallback, mirror AvatarMarker / ProfileSidebar `initialsOf`) + name; a search input filters by case-insensitive name substring. On pick, call an `onPick(personId)` prop (the consumer owns the drop coordinate + placement). Empty state: muted "No people yet. Create one with + New." with a shortcut to the create-person form (route through the existing create flow). All row/copy text renders as React children — NEVER `dangerouslySetInnerHTML` (T-03-01). NO new EntityType, NO new repository function.

    In `src/features/person-map/MapView.tsx`: wire the Person tool (already in the ToolPalette from 03-03) following the SAME one-shot pattern the Portal tool uses in 03-06: arming Person shows a placement affordance; the picker opens (owned here or lifted to App); when a person is picked, place the marker at the drop point via `upsertMarker({ kind: 'person', mapId: activeMap.id, personId, x: <image-space drop x>, y: <image-space drop y>, layerId: <activeLayerId> })` (convert the drop point through `coords.stageToImage`), then return to Select (one-shot). Accept a `focusMarkerId` input (or similar) so a jump-to-placement (Task 2) can select + center that marker — reuse the single-select state from 03-04 and center the viewport on the composed stage point.

    In `src/app/App.tsx`: mount/host the PersonPicker for the Person-tool flow and pass `activeMapId` through (already lifted in 03-02). Reuse the `handleSaved` center-of-map placement pattern (l.139-150) for any default-position fallback. (The "Appears on" jump wiring is added in Task 2.)

    Create `tests/features/personPicker.test.ts` (MAP-05): seed one Person and one map A; simulate the pick path by calling the same `upsertMarker({ kind:'person', mapId: A, personId, x, y, layerId })` the picker calls (no id) and assert exactly one Marker row exists for that personId on A with `kind === 'person'`; then place the SAME person on map B the same way and assert `db.markers.where('personId').equals(personId).count()` is 2 while `db.people.count()` is 1 (two placements, one canonical record — the MAP-05 contract, RESEARCH Pattern 4). Assert each marker carries the correct `mapId` and `kind === 'person'`.
  </action>
  <verify>
    <automated>npx vitest run tests/features/personPicker.test.ts --no-file-parallelism</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/personPicker.test.ts --no-file-parallelism` passes (pick = new Marker row; second placement = two markers, one Person)
    - `grep -n "upsertMarker" src/features/person-map/editor/PersonPicker.tsx` matches OR `grep -n "onPick" src/features/person-map/editor/PersonPicker.tsx` matches AND `grep -n "upsertMarker" src/features/person-map/MapView.tsx` matches (the pick places a marker through the repository)
    - `grep -nE "kind: ?'person'|kind:'person'" src/features/person-map/MapView.tsx` matches (Person tool places a person-kind marker)
    - `grep -vE '^\s*(//|/\*|\*)' src/features/person-map/editor/PersonPicker.tsx | grep -ci "dangerouslySetInnerHTML"` returns 0 (XSS-safe rows)
    - `grep -n "db.people" src/features/person-map/editor/PersonPicker.tsx` matches (reactive people list)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Person tool opens a searchable PersonPicker; picking a person drops a new Marker row on the active map via upsertMarker (one-shot); placing the same person on a second map yields two markers for one Person; placement is unit-tested per MAP-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile "Appears on:" list with jump-to-placement + canonical-record propagation test</name>
  <files>src/features/profile/ProfileSidebar.tsx, src/features/profile/ProfileSidebar.module.css, src/app/App.tsx, tests/features/appearsOn.test.ts</files>
  <read_first>
    - src/features/profile/ProfileSidebar.tsx (the WHOLE file — the People-only DATA-02 section l.237-265 where "Appears on" appends; the useLiveQuery reactive-read pattern l.140-154; the existing XSS-safe React-children rendering rule l.15-16; how it is entity-generic and only People should get this section)
    - src/features/browse/BrowseList.tsx (the onShowOnMap callback shape — the existing "show on map" plumbing this extends)
    - src/app/App.tsx (showOnMap l.167-172 — the existing jump that sets the map view; activeMapId/setActiveMapId from 03-02; how ProfileSidebar is mounted + its onShowOnMap/onOpenEntity props are wired)
    - src/db/schema.ts (the markers table indexed by personId + mapId — the index that makes db.markers.where('personId') cheap)
    - src/db/repository.ts (updateMap/updatePerson — for the propagation test: editing the Person leaves a single canonical record)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md ("Appears on" query l.326 — db.markers.where('personId').equals(id).toArray() grouped by mapId; Pattern 4 canonical-record propagation l.324)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (S4 profile sidebar extension l.253-256; the APPEARS ON eyebrow + jump-to-placement; copy "APPEARS ON" / "Not placed on any map yet." l.273-274; typography eyebrow l.118)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (ProfileSidebar/App.tsx "Appears on" assignment l.249-253; reactive reads l.283-285)
  </read_first>
  <behavior>
    - For a person placed on two maps, the "Appears on" query returns both placements grouped by mapId (one row per map).
    - For a person placed on no map, the section renders the muted "Not placed on any map yet." empty state.
    - Editing the canonical Person (e.g. name) leaves exactly one Person record while both placements (markers) continue to reference it — identity is shared, only x/y/size/rotation are per-placement (criterion 4).
    - Each "Appears on" row, when activated, requests a jump to that map + placement (callback fired with the target mapId and markerId).
  </behavior>
  <action>
    In `src/features/profile/ProfileSidebar.tsx`: add an "Appears on:" section rendered ONLY when `type === 'people'` (it has no meaning for maps/groups/relationship-links). Read the placements reactively: `useLiveQuery(() => db.markers.where('personId').equals(id).filter(m => m.kind === 'person').toArray(), [id])`, then group by `mapId` and resolve each map's name (a second reactive read of `db.maps` or a join — keep it cheap; people are placed on modest numbers of maps). Render an `APPEARS ON` eyebrow (13px/600 uppercase, +0.04em — UI-SPEC typography l.118) followed by one `<button>` per map: the map name as a React child (XSS-safe — NEVER `dangerouslySetInnerHTML`, consistent with the file's existing rule l.15-16), wired to a new `onJumpToPlacement(mapId, markerId)` prop. Empty (person exists, no placements): the muted "Not placed on any map yet." copy (UI-SPEC l.274). Add the `onJumpToPlacement?: (mapId: string, markerId: string) => void` prop to `ProfileSidebarProps`. Reference D-12 in a short head comment near the section. Do NOT change the existing destructive-action / lightbox / custom-field logic.

    In `src/app/App.tsx`: implement `onJumpToPlacement(mapId, markerId)` — set `activeMapId` (from 03-02) to `mapId`, switch to the Map view, and pass the target `markerId` to MapView so it selects + centers that placement (reuse/extend the existing `showOnMap` plumbing l.167-172 and the `focusMarkerId` input added to MapView in Task 1). Wire it into the `<ProfileSidebar>` render.

    Create `tests/features/appearsOn.test.ts` (MAP-05 / criterion 4): seed one Person placed on two maps (two Marker rows via `upsertMarker`, same personId, kinds 'person'); assert the "Appears on" grouping helper (extract a pure `groupPlacementsByMap(markers)` from the sidebar so it is unit-testable without rendering) returns two map groups for that person. Then edit the Person (e.g. rename via the repository) and assert: exactly one Person record still exists, both Marker rows still reference the same personId, and the placements are unchanged (per-placement x/y survive) — proving the canonical record propagates to every placement (RESEARCH Pattern 4, success criterion 4). Also assert a person with zero placements yields an empty grouping (drives the empty state).
  </action>
  <verify>
    <automated>npx vitest run tests/features/appearsOn.test.ts --no-file-parallelism</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/appearsOn.test.ts --no-file-parallelism` passes (two placements group into two maps; canonical-record edit leaves one Person + both markers; empty grouping for an unplaced person)
    - `grep -n "markers.where('personId')" src/features/profile/ProfileSidebar.tsx` matches (the Appears-on query)
    - `grep -ni "appears on" src/features/profile/ProfileSidebar.tsx` matches (the section + eyebrow)
    - `grep -n "onJumpToPlacement" src/features/profile/ProfileSidebar.tsx` matches and `grep -n "onJumpToPlacement" src/app/App.tsx` matches (jump wired end to end)
    - `grep -vE '^\s*(//|/\*|\*)' src/features/profile/ProfileSidebar.tsx | grep -ci "dangerouslySetInnerHTML"` returns 0 (map names are XSS-safe)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The People profile shows an "Appears on:" list (grouped by map) with jump-to-placement that sets the map active and centers the placement; the empty state shows when unplaced; a unit test proves grouping and canonical-record propagation across placements (criterion 4).</done>
</task>

<task type="auto">
  <name>Task 3: E2E — place an existing person on two maps, edit propagates, jump from "Appears on"</name>
  <files>e2e/place-person.spec.ts</files>
  <read_first>
    - e2e/marker.spec.ts (the existing marker E2E — window.__rb bridge, seeding people/maps/markers, canvas + sidebar selectors)
    - e2e/map-switch.spec.ts (the active-map / breadcrumb assertions from 03-02 — how the active map is asserted)
    - e2e/draw-shapes.spec.ts (tool-palette interaction harness from 03-03)
    - src/features/person-map/editor/PersonPicker.tsx + src/features/profile/ProfileSidebar.tsx + MapView.tsx + App.tsx (Tasks 1-2 — selectors, the Appears-on rows, the Person tool)
    - .planning/phases/03-map-editor-spaces-navigation/03-VALIDATION.md (the MAP-05 row + the most-important-tests "multi-placement propagation" item)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 4; the most-important-tests #3 multi-placement propagation l.608)
  </read_first>
  <action>
    Create `e2e/place-person.spec.ts` (MAP-05, criterion 4): via `window.__rb` seed two maps (A, B) and one Person; open the app on map A; arm the Person tool, open the PersonPicker, pick the person, and drop a marker on the canvas — assert a person-kind marker now exists on A (read back via the bridge / assert canvas object count). Switch to map B (MapSwitcher) and place the SAME person — assert two Marker rows now exist for one personId and exactly one Person record (the multi-placement contract). Open the person's profile (marker click or browse-row) and assert the "Appears on:" list shows BOTH maps A and B. Click an "Appears on" row for the other map and assert the active map switches to it and the placement is selected/centered. Finally edit the person's name via the bridge/UI and assert both placements still reference the one canonical Person (the rendered name updates everywhere). If precise canvas drop is brittle in Playwright, route the placement through a bridge-exposed helper that calls the same `upsertMarker` the picker uses, keeping the assertion on the observable multi-placement + Appears-on + propagation behavior. Mirror the existing E2E harness; route all state through the repository (NEVER straight to Dexie).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx playwright test e2e/place-person.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/place-person.spec.ts` exists and asserts: place a person on map A; place the same person on map B → two markers, one Person; "Appears on" lists both maps; an Appears-on row jumps to + centers the placement; editing the person propagates to every placement
    - Uses the `window.__rb` bridge; routes state through the repository (no direct Dexie writes)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>An E2E proves the full MAP-05 user flow: placing an existing person on multiple maps from the map side, the profile "Appears on" list + jump-to-placement, and canonical-record propagation across placements (criterion 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| person name / map name → picker rows + "Appears on" list (DOM) | Untrusted user text crosses into rendered React rows/buttons |
| stored Marker.personId / Marker.mapId → "Appears on" query + jump navigation | A dangling personId/mapId (placement on a deleted map) crosses into the grouping + navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Person names in PersonPicker rows + map names in the "Appears on" list (an XSS could exfiltrate the Drive token) | mitigate | ALL row/list text renders as React children — NEVER `dangerouslySetInnerHTML` (consistent with ProfileSidebar l.15-16 and BrowseList l.11); picker + Appears-on tests/grep assert no innerHTML usage |
| T-03-10 | Tampering / availability | An "Appears on" placement whose `mapId` references a deleted map | mitigate | The grouping resolves map names defensively; a missing map is skipped or shown as a muted "(deleted map)" row rather than crashing the sidebar (mirrors the portal deleted-target handling in 03-06) |
| T-03-04 | Denial of Service | A very large people list rendered in the PersonPicker | mitigate | The picker reuses BrowseList's modest-scroll/windowing approach + live search filter so the list stays bounded (UI-SPEC S16; people lists are v1-modest) |
| T-03-SC | Tampering | npm installs (@radix-ui/react-popover optional) | mitigate | RESEARCH Package Legitimacy Audit clears @radix-ui/react-popover OK (A6); prefer the already-installed `@radix-ui/react-dialog` to avoid any new dependency — no install needed for this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` green
- `npx vitest run tests/features/personPicker.test.ts tests/features/appearsOn.test.ts --no-file-parallelism` green
- `npx playwright test e2e/place-person.spec.ts` passes
- Manual: open a map, arm the Person tool, pick a person from the picker, drop a marker; switch maps and place the same person again; open the person's profile and see "Appears on: A, B"; click a row to jump + center; rename the person and confirm both placements update
</verification>

<success_criteria>
- The map-side PersonPicker places an existing person as a new Marker row on the active map (D-11)
- Placing one person on multiple maps yields multiple markers for one canonical Person (D-13, criterion 4)
- The profile "Appears on:" list shows every map a person is placed on, with jump-to-placement (D-12)
- Editing a person propagates to every placement (success criterion 4)
- Placement + propagation are unit-tested and the full flow is E2E-covered
</success_criteria>

<output>
Create `.planning/phases/03-map-editor-spaces-navigation/03-07-SUMMARY.md` when done.
</output>

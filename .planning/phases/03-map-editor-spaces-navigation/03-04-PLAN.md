---
phase: 03-map-editor-spaces-navigation
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/features/person-map/editor/TransformerOverlay.tsx
  - src/features/person-map/AvatarMarker.tsx
  - src/features/person-map/MapView.tsx
  - tests/features/transformerOverlay.test.ts
  - e2e/transform-marker.spec.ts
  - e2e/transform-background.spec.ts
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "A user can select a placed object (marker, shape) and resize + rotate it via on-canvas handles"
    - "Resize/rotate persists across reload: width/height/rotation are saved on the marker (or geometry on the shape)"
    - "The Transformer resets scaleX/scaleY to 1 on transform-end and bakes the change into width/height (strokes stay uniform)"
    - "A user can resize/rotate the background image, and the transform persists on the MapDoc"
    - "When the background is re-fit, already-placed markers stay anchored to their physical spot (image-space)"
    - "Single-select only: selecting one object shows its handles; clicking empty canvas deselects"
  artifacts:
    - path: "src/features/person-map/editor/TransformerOverlay.tsx"
      provides: "a single Konva.Transformer attached imperatively to the selected node; scale-reset-to-1 persist (RESEARCH Pattern 1)"
    - path: "src/features/person-map/AvatarMarker.tsx"
      provides: "marker consumes width/height/rotation, composes x/y through the background transform, exposes a ref for the Transformer; optional name label (D-20 default hidden)"
      contains: "rotation"
    - path: "src/features/person-map/MapView.tsx"
      provides: "single-select state + TransformerOverlay in L2; background-transform affordance with image-space anchoring"
    - path: "tests/features/transformerOverlay.test.ts"
      provides: "unit test for the scale-reset-to-1 persist logic (baked width/height, rotation passthrough, MIN clamp, image-space x/y, correct persist branch)"
      contains: "computeTransformPersist"
  key_links:
    - from: "src/features/person-map/editor/TransformerOverlay.tsx"
      to: "src/db/repository.ts"
      via: "onTransformEnd resets scale and persists width/height/rotation via upsertMarker / updateMap"
      pattern: "(upsertMarker|updateMap)"
    - from: "src/features/person-map/AvatarMarker.tsx"
      to: "src/features/person-map/coords.ts"
      via: "marker x/y composed through map.backgroundTransform via imageToStage; drag-end converts back via stageToImage"
      pattern: "(imageToStage|stageToImage)"
---

<objective>
Deliver the deferred Phase-1 UAT criteria 6 & 7 as a vertical slice: a single Konva `Transformer` overlay giving resize + rotate handles to any selected object (D-14/D-15), the marker consuming its persisted width/height/rotation, and a background-image transform that keeps already-placed markers anchored in image space (D-16, the subtle coordinate-model payoff). After this plan a user can resize/rotate markers and shapes and re-fit the background without losing placements — capability they did not have before.

Purpose: RESEARCH Pattern 1 (Transformer, scale-reset-to-1) and Pattern 7 (image-space anchoring) converge here; the 03-01 migration test already proved the identity case, this plan proves the non-identity case end-to-end. Builds on the drawing slice (03-03).
Output: `TransformerOverlay`, an upgraded `AvatarMarker`, MapView select+background-transform wiring, and tests + E2E.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-map-editor-spaces-navigation/03-CONTEXT.md
@.planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md
@.planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md
@.planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md
@.planning/phases/03-map-editor-spaces-navigation/03-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/features/person-map/editor/TransformerOverlay.tsx` — a single `Konva.Transformer` attached via ref + `useEffect` to the selected node (no pure-declarative way, RESEARCH Pattern 1). On `transformend`: read scaleX/scaleY, reset both to 1, bake into width/height, persist via the right repository call. Amber handles; `anchorSize` 24 + enlarged hit on coarse pointers (D-19); `rotateEnabled`, `flipEnabled={false}`, `boundBoxFunc` enforcing a minimum size. The attach effect is idempotent (RESEARCH Pitfall 1, StrictMode double-run).
- `AvatarMarker.tsx` (MODIFIED) — composes `x/y` through `coords.imageToStage(map.backgroundTransform)`; converts back via `stageToImage` on drag-end; consumes `width`/`height`/`rotation` (applied to the marker Group); exposes a ref so the Transformer can attach (RESEARCH A3: attach to the Group first; if the stem scales badly attach to an inner node); adds an optional name-label child gated by a `showLabels` prop (D-20 default hidden), rendered XSS-safe.
- `MapView.tsx` (MODIFIED) — single-select state (`selectedObjectId` + kind), TransformerOverlay mounted in the L2 overlay layer attached to the selected node, click-empty-canvas to deselect (D-15); a background-transform affordance ("Edit background" toggle) that mounts a Transformer on the background image and persists `MapDoc.backgroundTransform` via `updateMap`, with the "markers stay anchored" hint.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TransformerOverlay (scale-reset-to-1 persist) + its test</name>
  <files>src/features/person-map/editor/TransformerOverlay.tsx, tests/features/transformerOverlay.test.ts</files>
  <read_first>
    - src/features/person-map/AvatarMarker.tsx (ref/getStage usage, the drag-persist pattern l.42-50)
    - src/db/repository.ts (upsertMarker extended in 03-01 — accepts width/height/rotation; updateMap for shapes)
    - src/app/tokens.ts (colors.amber — handles are amber)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 1 FULL code l.233-283 — ref attach, onTransformEnd scale reset, boundBoxFunc, anchorSize coarse; Anti-pattern: never persist raw scale l.398; Pitfall 1 react-konva prop-reset l.452-456)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (S18 Transformer handles l.238-244 — amber, 12px desktop / 24px touch, 44px hit, single-select)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (TransformerOverlay assignment l.208-211)
  </read_first>
  <action>
    Create `src/features/person-map/editor/TransformerOverlay.tsx`. Props: the selected node ref (or selected object id + a lookup), the object kind ('person'|'portal'|'shape'), and the map id. Attach a single `Konva.Transformer` via `trRef` + `useEffect` that calls `trRef.current.nodes([selectedNode])` + `getLayer()?.batchDraw()` when a node is selected, and clears nodes when none (RESEARCH Pattern 1). Make the effect idempotent so React StrictMode's double-invoke does not double-attach (RESEARCH Pitfall 1). On `transformend`: read `node.scaleX()/scaleY()`, set both back to 1, compute `width = max(MIN, node.width()*scaleX)`, `height = max(MIN, node.height()*scaleY)`, `rotation = node.rotation()`; persist — for a marker via `upsertMarker({ id, mapId, personId, targetMapId, kind, x: <stageToImage of node x/y>, y, width, height, rotation })`, for a shape via `updateMap(mapId, { shapes: <array with this shape's geometry+rotation updated> })`. NEVER persist raw scale (RESEARCH Anti-pattern). Configure `<Transformer>` with `rotateEnabled`, `flipEnabled={false}`, `anchorStroke`/`borderStroke` = `colors.amber`, `anchorSize` 24 on `pointer: coarse` else 12, an enlarged hit area for touch (44px), and a `boundBoxFunc` that returns the old box when the new box is below MIN width/height.

    Create `tests/features/transformerOverlay.test.ts`: unit-test the persist logic by extracting a pure helper `computeTransformPersist(node-like, kind)` (or test the onTransformEnd handler with a fake node exposing scaleX/scaleY/width/height/rotation/x/y). Assert: given scaleX=2, width=50 → persisted width=100 and scale is reset to 1 (the helper returns width 100, not a scale); rotation passes through; below-MIN is clamped to MIN; for a marker the persisted x/y are the `stageToImage`-converted values (image-space). Assert the marker branch calls the upsertMarker-shaped payload and the shape branch the updateMap-shaped payload (spy/mocks).
  </action>
  <verify>
    <automated>npx vitest run tests/features/transformerOverlay.test.ts --no-file-parallelism</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/transformerOverlay.test.ts --no-file-parallelism` passes (scale-reset-to-1 → baked width, rotation passthrough, MIN clamp, image-space x/y, correct persist branch)
    - `grep -nE "scaleX\(1\)|scaleX\(\s*1\s*\)|\.scaleX\(1\)" src/features/person-map/editor/TransformerOverlay.tsx` matches OR the reset-to-1 is otherwise present (verify the node's scale is reset, not persisted)
    - `grep -n "boundBoxFunc" src/features/person-map/editor/TransformerOverlay.tsx` matches (min-size guard)
    - `grep -n "colors.amber" src/features/person-map/editor/TransformerOverlay.tsx` matches (amber handles)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TransformerOverlay attaches a single amber Transformer imperatively, resets scale to 1 and bakes width/height + rotation, persists through the repository (image-space for markers), guards a min size, and its persist logic is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade AvatarMarker (image-space compose, transform fields, name label) + wire select + background transform in MapView</name>
  <files>src/features/person-map/AvatarMarker.tsx, src/features/person-map/MapView.tsx</files>
  <read_first>
    - src/features/person-map/AvatarMarker.tsx (the WHOLE file — Group x/y l.54-55, handleDragEnd l.42-50, click/tap l.58-59, the initials Text block l.97-109, the composite Group structure l.52-126)
    - src/features/person-map/MapView.tsx (as modified by 03-03 — three-layer structure, L2 overlay layer, tool-mode wiring, select-on-click)
    - src/features/person-map/coords.ts (imageToStage/stageToImage)
    - src/features/person-map/editor/TransformerOverlay.tsx (Task 1)
    - src/db/repository.ts (upsertMarker extended; updateMap for backgroundTransform)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 7 background transform + anchoring l.374-394; Pattern 1 AvatarMarker composite note l.283 / A3 l.541; Pitfall 1 persist-on-transform-end l.452-456)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (S16b background transform l.246-251; S18 single-select; canvas name-label spec l.120; D-20 default-hidden labels l.299)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (AvatarMarker assignment l.171-190)
  </read_first>
  <action>
    In `src/features/person-map/AvatarMarker.tsx`: take the map's `backgroundTransform` as a prop (or the composed stage point) and set the Group `x`/`y` from `coords.imageToStage(marker, backgroundTransform)` instead of raw `marker.x/y`. In `handleDragEnd`, convert `e.target.x()/y()` back via `coords.stageToImage(..., backgroundTransform)` BEFORE calling `upsertMarker` (so image-space stays the stored model — RESEARCH Pattern 7). Apply `marker.width`/`height`/`rotation` when present (rotation on the Group; width/height drive the avatar sizing or are passed to the Transformer attach node — follow RESEARCH A3: attach Transformer to the Group first, fall back to an inner sizing node if the stem scales badly). Expose a `ref` (forwardRef or an `onNodeRef` callback) so the TransformerOverlay can attach to the selected marker. Add an optional name-label child: a Konva `Text` (the person name) below the pin-stem, gated by a `showLabels` prop (default false, D-20), rendered XSS-safe exactly like the initials Text (l.97-109) — NEVER `dangerouslySetInnerHTML`. Persist transform fields on `onTransformEnd` is handled by TransformerOverlay (the marker just exposes the node + consumes the fields on the next render — RESEARCH Pitfall 1: the next useLiveQuery render supplies matching props).

    In `src/features/person-map/MapView.tsx`: add single-select state (`selectedObjectId` + its kind). Selecting a marker/shape sets it; clicking empty canvas clears it (D-15, extend the existing empty-canvas onClick l.137-139). Mount `TransformerOverlay` in the L2 overlay layer, attached to the selected node. Pass each map's `backgroundTransform` to its `AvatarMarker`s and `ShapeNode`s. Add a background-transform affordance: an "Edit background" toggle (UI-SPEC S16b — select the background with the Select tool when no object is hit, or an explicit toggle to avoid accidental grabs) that mounts a Transformer on the `<KonvaImage>` and, on transform-end, persists `MapDoc.backgroundTransform` via `updateMap`; show the "Transforming background — markers stay anchored." hint chip while active (UI-SPEC copy l.275). Because markers compose through the background transform, re-fitting keeps them anchored automatically (no per-marker rewrite). Pass `showLabels` down (wired to a toggle in the layers panel in 03-05; default false for now).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run tests/features --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "imageToStage|stageToImage" src/features/person-map/AvatarMarker.tsx` matches (image-space compose on render + convert-back on drag-end)
    - `grep -nE "marker\.(width|height|rotation)|rotation" src/features/person-map/AvatarMarker.tsx` matches (transform fields consumed)
    - `grep -vE '^\s*(//|/\*|\*)' src/features/person-map/AvatarMarker.tsx | grep -ci "dangerouslySetInnerHTML"` returns 0 (name label is XSS-safe)
    - `grep -n "TransformerOverlay" src/features/person-map/MapView.tsx` matches and `grep -nE "selectedObject|backgroundTransform" src/features/person-map/MapView.tsx` matches
    - `npx tsc --noEmit` exits 0; existing marker tests still pass
  </acceptance_criteria>
  <done>AvatarMarker composes/persists in image space, consumes width/height/rotation, exposes a ref, and has an optional default-hidden name label; MapView does single-select, mounts the TransformerOverlay, and supports a persisted background transform that keeps markers anchored.</done>
</task>

<task type="auto">
  <name>Task 3: E2E — resize/rotate a marker persists; background transform persists with markers anchored</name>
  <files>e2e/transform-marker.spec.ts, e2e/transform-background.spec.ts</files>
  <read_first>
    - e2e/marker.spec.ts (the existing marker E2E — window.__rb bridge, how a marker is seeded + asserted, canvas interaction selectors)
    - e2e/map-create.spec.ts (map seeding via the bridge)
    - src/features/person-map/editor/TransformerOverlay.tsx + AvatarMarker.tsx + MapView.tsx (Tasks 1-2 — what the handles look like, how select works, the background-transform toggle)
    - tests/features/markerTransform.roundtrip.test.ts + tests/features/bgTransform.anchor.test.ts (the 03-01 round-trip assertions — the E2E proves the same end-to-end through the UI)
    - .planning/phases/03-map-editor-spaces-navigation/03-VALIDATION.md (criteria 6 & 7 test rows; the most-important-tests list)
  </read_first>
  <action>
    Create `e2e/transform-marker.spec.ts` (criterion 6): seed a map + a person marker via `window.__rb`; open the app, single-click the marker to select it (handles appear), drag a Transformer corner handle to resize and/or rotate, then reload the page and assert the marker's persisted `width`/`height`/`rotation` survive (read back via the bridge, or assert the rendered size/rotation). If precise handle-dragging is brittle in Playwright against a canvas, drive the transform through the same repository call the handle triggers (a bridge-exposed `__rb.transformMarker(id, {width,height,rotation})`) and assert persistence across reload — keep the assertion on PERSISTENCE, which is the criterion.

    Create `e2e/transform-background.spec.ts` (criterion 7): seed a map + a marker at a known image-space point; enter the background-transform affordance, apply a scale/rotation to the background (via handle drag or a bridge-exposed `__rb.setBackgroundTransform(mapId, t)`), assert the transform persists across reload (read back), AND assert the marker stays anchored — its image-space `x/y` are UNCHANGED while its on-screen (stage) position moved with the image (assert image-space coords unchanged + composed stage point changed). This is the end-to-end proof of the D-16 anchoring model that 03-01 proved at the unit level.

    Both specs mirror the existing E2E harness (bridge seeding, no real Drive). Add the minimal `window.__rb` helpers needed (transformMarker / setBackgroundTransform) to the test bridge if they do not already exist, routing through the repository (NEVER straight to Dexie).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx playwright test e2e/transform-marker.spec.ts e2e/transform-background.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/transform-marker.spec.ts` exists and asserts width/height/rotation persist across a reload
    - `e2e/transform-background.spec.ts` exists and asserts (a) backgroundTransform persists across reload AND (b) the marker's image-space x/y are unchanged after the background transform (anchoring)
    - Both specs use the `window.__rb` bridge and route any transform through the repository (no direct Dexie writes)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Two E2E specs prove criterion 6 (marker resize/rotate persists) and criterion 7 (background transform persists AND markers stay anchored) end-to-end through the UI/bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored transform fields → render/Transformer | Corrupt width/height/rotation/backgroundTransform from cloud/backup cross into the canvas math |
| user transform gesture → repository | A transform write must go through the validate→stamp→emit path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | Marker width/height/rotation + MapDoc.backgroundTransform read from cloud/backup | mitigate | Validated by MarkerSchema/BackgroundTransformSchema on load (03-01, optional-with-default); boundBoxFunc + MIN clamp prevent degenerate sizes at edit time |
| T-03-13 | Tampering | A transform write bypassing the repository | mitigate | TransformerOverlay persists ONLY via upsertMarker/updateMap (validate→stamp→emit); E2E bridge routes through the repository; never writes straight to Dexie (RESEARCH Pitfall 1 also keeps react-konva render in sync) |
| T-03-07 | Tampering | Background transform shifting existing markers | mitigate | Image-space anchoring: markers compose through the transform, so a re-fit changes on-screen position but not stored image-space coords; transform-background E2E asserts this |
| T-03-SC | Tampering | npm installs | accept | No new package installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` green
- `npx vitest run tests/features --no-file-parallelism` green (transformerOverlay + the rest)
- `npx playwright test e2e/transform-marker.spec.ts e2e/transform-background.spec.ts` passes
- Manual: select a marker, resize + rotate, reload — it persists; resize the background — the lobby person stays in the lobby
</verification>

<success_criteria>
- A single amber Transformer gives resize+rotate to any selected object; scale resets to 1 and bakes into width/height
- Marker width/height/rotation persist across reload AND export/restore (03-01 round-trip + this E2E)
- Background transform persists; markers stay anchored in image space when the background is re-fit
- Single-select only; click empty canvas deselects
</success_criteria>

<output>
Create `.planning/phases/03-map-editor-spaces-navigation/03-04-SUMMARY.md` when done.
</output>

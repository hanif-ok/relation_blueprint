---
phase: 03-map-editor-spaces-navigation
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/features/person-map/editor/useToolMode.ts
  - src/features/person-map/editor/ToolPalette.tsx
  - src/features/person-map/editor/ToolPalette.module.css
  - src/features/person-map/editor/ShapeNode.tsx
  - src/features/person-map/editor/ZoneLabel.tsx
  - src/features/person-map/editor/StylePopover.tsx
  - src/features/person-map/editor/StylePopover.module.css
  - src/features/person-map/MapView.tsx
  - src/app/tokens.css
  - tests/features/useToolMode.test.ts
  - tests/features/shapes.test.ts
  - e2e/draw-shapes.spec.ts
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "A user can pick a drawing tool (Rect/Ellipse/Line/Polygon) and draw a shape on the canvas, which persists"
    - "Select is the default tool; in Select mode dragging empty canvas pans, dragging an object moves it"
    - "In a draw mode, single-pointer drag draws the shape (does NOT pan); a two-finger drag still pans"
    - "A drawn shape can be styled from a preset palette (Stone/Sage/Clay/Dusk/Plum) with fill on/off and an optional zone label"
    - "A shape carrying a non-empty label renders its label as a Konva Text chip (never dangerouslySetInnerHTML)"
    - "Drawn shapes persist on the MapDoc and survive reload (written through updateMap)"
  artifacts:
    - path: "src/features/person-map/editor/useToolMode.ts"
      provides: "the pan/draw/select tool-mode interaction state machine (toggles Stage.draggable by tool)"
      exports: ["useToolMode"]
    - path: "src/features/person-map/editor/ToolPalette.tsx"
      provides: "Select/Rect/Ellipse/Line/Polygon/Portal/Person tool buttons (D-17)"
    - path: "src/features/person-map/editor/ShapeNode.tsx"
      provides: "renders a Rect/Ellipse/Line/Polygon from a Shape descriptor; drag-persists via updateMap"
    - path: "src/features/person-map/editor/StylePopover.tsx"
      provides: "preset palette + fill toggle + zone label editor (D-02/D-03)"
  key_links:
    - from: "src/features/person-map/MapView.tsx"
      to: "src/features/person-map/editor/useToolMode.ts"
      via: "the active tool toggles Stage.draggable and routes pointer events to draw vs select"
      pattern: "useToolMode"
    - from: "src/features/person-map/editor/ShapeNode.tsx"
      to: "src/db/repository.ts"
      via: "shape geometry persists through updateMap (MapDoc.shapes sub-object), never straight to Dexie"
      pattern: "updateMap"
    - from: "src/features/person-map/editor/ZoneLabel.tsx"
      to: "react-konva Text"
      via: "user label rendered as Konva Text child only (XSS-safe, T-03-01)"
      pattern: "Text"
---

<objective>
Deliver the MAP-02 vertical slice: a tool palette (D-17), the pan/draw/select interaction state machine with touch parity wired in from the start (D-19), shape drawing for Rect/Ellipse/Line/Polygon (D-01), and a minimal style popover with the five-preset palette + fill toggle + zone label (D-02/D-03). After this plan a user can draw and style rooms/areas/zones on a map — capability they did not have before.

Purpose: This is the heart of the editor's custom logic (RESEARCH "Don't Hand-Roll" key insight: the tool-mode/gesture state machine is one of the two genuinely-new pieces). It builds on the active-map model + coords from 03-02.
Output: `useToolMode`, `ToolPalette`, `ShapeNode`, `ZoneLabel`, `StylePopover`, MapView draw-wiring, the zone/portal tokens mirrored into `tokens.css`, and tests.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-map-editor-spaces-navigation/03-CONTEXT.md
@.planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md
@.planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md
@.planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md
@.planning/phases/03-map-editor-spaces-navigation/03-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/features/person-map/editor/useToolMode.ts` — `useToolMode()` returning the active `tool` ('select'|'rect'|'ellipse'|'line'|'polygon'|'portal'|'person'), a setter, and helpers: `isDrawMode`, and a `stageDraggable` boolean derived from tool + whether an object is being dragged (RESEARCH Pattern 2 / Pitfall 3). Holds in-progress draw state (start point in image-space, live preview, polygon vertex list).
- `src/features/person-map/editor/ToolPalette.tsx` (+ `.module.css`) — the seven tool buttons (D-17), active tool gets a 2px amber bar, single-key shortcuts in tooltips (V/R/O/L/P/T/M), 44px/48px touch hit areas, arrow-key roving focus, disabled-with-reason when no active map.
- `src/features/person-map/editor/ShapeNode.tsx` — renders one `Shape` descriptor as a Konva `Rect`/`Ellipse`/`Line`/`Line closed`; reads geometry in image-space, composes through `coords.imageToStage`; drag-persists via `updateMap`.
- `src/features/person-map/editor/ZoneLabel.tsx` — a Konva `Text` chip (paper-shade pill) for a shape's `label` (D-02); XSS-safe, no innerHTML.
- `src/features/person-map/editor/StylePopover.tsx` (+ `.module.css`) — Radix Popover/Dialog: 5-swatch preset palette + fill on/off + label input + (later) move-to-layer; writes via `updateMap`.
- `MapView.tsx` (MODIFIED) — hosts the tool palette, routes Stage pointer events through `useToolMode` to draw/commit shapes, renders `ShapeNode` children in L1, opens the StylePopover on shape select.
- `tokens.css` (MODIFIED) — portal hue + 5 zone preset hexes mirrored from `tokens.ts` (canvas/DOM parity, UI-SPEC l.82).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useToolMode interaction state machine (pan/draw/select + touch disambiguation) + its test</name>
  <files>src/features/person-map/editor/useToolMode.ts, tests/features/useToolMode.test.ts</files>
  <read_first>
    - src/features/person-map/MapView.tsx (as modified by 03-02 — handleWheel, the Stage event wiring, three-layer structure)
    - src/features/person-map/coords.ts (stageToImage — draw start/move points are captured in image space)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 2 draw state machine l.285-306; Pattern 6 gesture disambiguation table l.351-358; Pitfall 3 single-finger-draw-pans l.463-466; Pitfall 4 pinch jump l.468-471; Code Examples Stage.draggable toggle l.518-522)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (Touch & Desktop Parity Contract l.305-312; tool list + armed-state rules S10 l.189-195)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (useToolMode assignment l.215-218; No-Analog note l.293-296)
  </read_first>
  <action>
    Create `src/features/person-map/editor/useToolMode.ts`. Export `useToolMode()` returning: `tool` (the seven-value union, default `'select'`), `setTool`, `isDrawMode` (tool ∈ rect/ellipse/line/polygon), and a derived `stageDraggable` flag = `tool === 'select' && !objectIsBeingDragged && !twoFingerActive ? true : (twoFingerActive ? true : false)` — i.e. in Select mode the Stage pans on empty-canvas drag, in draw modes the Stage is NOT draggable so a single-pointer drag DRAWS (RESEARCH Pitfall 3), and a two-finger gesture ALWAYS pans/pinches regardless of mode (RESEARCH Pattern 6). Track in-progress draw state: `drawStart` (image-space point), `previewShape` (live), and for polygon a `vertices: number[]` accumulator (click adds a vertex, dblclick/Enter closes, Esc cancels). Expose handlers the Stage will call: `onPointerDown/Move/Up` (or return reducers) that, by tool, either select/pan (select mode) or start/update/commit a shape (draw modes). Armed-state semantics (UI-SPEC S10): Person/Portal are one-shot (return to Select after one drop — handled by the consumer), Rect/Ellipse/Line stay armed for repeated draws, Polygon stays armed until close. Provide a two-finger detection seam (a `setTwoFingerActive` the touch handler toggles) and, when the second touch lands, signal the consumer to `stage.stopDrag()` (RESEARCH Pitfall 4) — expose a flag/callback rather than touching Konva directly so the hook stays testable.

    Create `tests/features/useToolMode.test.ts`: drive the hook (via `@testing-library/react` renderHook or a plain reducer test if the state machine is extracted as a pure function): assert default tool is `'select'`; assert `stageDraggable` is true in select mode (no object drag) and false in each draw mode; assert two-finger-active forces `stageDraggable` true even in a draw mode; simulate a rect draw (down → move → up) and assert it produces a committed shape descriptor with image-space geometry above the size threshold, and that a below-threshold drag commits nothing; simulate polygon vertex accumulation + close. Keep the core state machine a pure, exported function so it can be tested without a DOM where practical.
  </action>
  <verify>
    <automated>npx vitest run tests/features/useToolMode.test.ts --no-file-parallelism</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/useToolMode.test.ts --no-file-parallelism` passes (default select, draggable-by-mode, two-finger override, rect draw threshold, polygon accumulate/close)
    - `grep -n "export function useToolMode" src/features/person-map/editor/useToolMode.ts` matches
    - `grep -nE "stageDraggable|isDrawMode" src/features/person-map/editor/useToolMode.ts` matches (the draggable-by-tool derivation exists)
    - `grep -nE "stopDrag|twoFinger" src/features/person-map/editor/useToolMode.ts` matches (two-finger pinch seam present)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>useToolMode is the tested pan/draw/select state machine: Select is default, draw modes suppress Stage panning for single-pointer, two-finger always pans/pinches, shapes commit above a size threshold, polygon accumulates and closes.</done>
</task>

<task type="auto">
  <name>Task 2: ToolPalette + ShapeNode + ZoneLabel + StylePopover; mirror tokens to tokens.css</name>
  <files>src/features/person-map/editor/ToolPalette.tsx, src/features/person-map/editor/ToolPalette.module.css, src/features/person-map/editor/ShapeNode.tsx, src/features/person-map/editor/ZoneLabel.tsx, src/features/person-map/editor/StylePopover.tsx, src/features/person-map/editor/StylePopover.module.css, src/app/tokens.css</files>
  <read_first>
    - src/features/nav/ViewSwitcher.tsx + src/features/nav/NewEntityMenu.tsx (segmented control + Radix surface analogs for ToolPalette/StylePopover)
    - src/features/person-map/AvatarMarker.tsx (the draggable-Group + drag-persist template l.42-50; the initials Konva Text block l.97-109 for ZoneLabel's XSS-safe text)
    - src/features/person-map/coords.ts (imageToStage for ShapeNode geometry composition)
    - src/app/tokens.ts (colors incl. the portal hue + zonePresets added in 03-01) and src/app/tokens.css (the :root custom properties to mirror the new hexes into)
    - src/db/repository.ts (updateMap l.263-276 — shapes persist through it; UpdateMapPatch l.259)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (S10 tool palette l.189-195; S17 style popover l.230-236; the zone preset palette table l.152-158; canvas text spec l.120; Icon library glyph names l.79)
    - .planning/phases/03-map-editor-spaces-navigation/03-PATTERNS.md (ShapeNode/ZoneLabel l.201-204; ToolPalette l.236-238; StylePopover via NewEntityMenu analog l.44)
  </read_first>
  <action>
    Create `ToolPalette.tsx` (+ `.module.css`): seven icon buttons (Lucide: `MousePointer2` Select, `Square` Rect, `Circle` Ellipse, `Slash` Line, `Pentagon` Polygon, `DoorOpen` Portal, `UserPlus` Person — UI-SPEC l.79), left→right with Select first/default. Active tool gets a 2px amber left-bar/underline + paper-shade fill (amber = active tool, UI-SPEC Color reserved-list item 2); rest buttons are ink glyph, paper-shade on hover. 44px hit / 48px on `pointer: coarse`. Single-key shortcut shown in each tooltip (V/R/O/L/P/T/M). Arrow-key roving focus within the group (one tab stop). Disabled-with-reason ("Open or create a map first") when no active map. Driven by `useToolMode`'s `tool`/`setTool` (passed as props from MapView).

    Create `ShapeNode.tsx`: render a `Shape` descriptor as the matching Konva primitive — `Rect` (rect), `Ellipse` (ellipse), `Line` (line, open), `Line` with `closed` (polygon). Read geometry in image-space, compose the origin through `coords.imageToStage(shape, map.backgroundTransform)` and apply `scale`/`rotation` from the transform + the shape's own `rotation`. Apply the preset stroke + (if `fill` on) the 20% fill from `tokens.zonePresets[shape.preset]`. Line shapes get a generous `hitStrokeWidth` (touch grab, UI-SPEC l.311). Draggable; on drag-end convert back via `coords.stageToImage` and persist via `updateMap(mapId, { shapes: <updated array> })` (NEVER straight to Dexie — RESEARCH anti-pattern; PATTERNS l.204). Single-click selects the shape (consumer opens StylePopover + Transformer).

    Create `ZoneLabel.tsx`: a Konva `Text` chip — paper-shade `#E7E2D6` pill (a `Rect` behind a `Text`), Inter 600 13px ink `#26211A`, centered on the shape, rendered ONLY when `shape.label` is non-empty (D-02). User text passes straight into the Konva `Text` `text` prop — NEVER `dangerouslySetInnerHTML` (threat T-03-01; mirror AvatarMarker initials text l.97-109).

    Create `StylePopover.tsx` (+ `.module.css`): Radix Popover (or Dialog if Popover not installed — RESEARCH says Popover optional, fall back to the installed Dialog) opened when a shape is selected. Contents: a 5-swatch preset palette (Stone/Sage/Clay/Dusk/Plum) single-select with an amber selection ring on the current; a Fill on/off toggle (forced off + disabled for `line` shapes per UI-SPEC l.160); a Label text input (sets `shape.label` → renders the ZoneLabel chip). NO color picker / stroke width / opacity / dashes (D-03 deferred). Each change writes through `updateMap`. (Move-to-layer dropdown is added in 03-05; leave a clearly-marked seam.)

    In `src/app/tokens.css`: add the portal hue and the five zone-preset hexes as `:root` custom properties mirroring `tokens.ts` exactly (UI-SPEC l.82 — canvas reads tokens.ts, DOM reads tokens.css, one source of truth; never inline a hex in a component). <!-- planner-discipline-allow: #3E6B8C -->
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run tests/features --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "MousePointer2|Square|Circle|Slash|Pentagon|DoorOpen|UserPlus" src/features/person-map/editor/ToolPalette.tsx` returns 7 (all seven tool glyphs)
    - `grep -nE "Rect|Ellipse|Line" src/features/person-map/editor/ShapeNode.tsx` matches (all primitives) and `grep -n "updateMap" src/features/person-map/editor/ShapeNode.tsx` matches (persists via repository)
    - `grep -vE '^\s*(//|/\*|\*)' src/features/person-map/editor/ZoneLabel.tsx | grep -ci "dangerouslySetInnerHTML"` returns 0 AND `grep -n "Text" src/features/person-map/editor/ZoneLabel.tsx` matches
    - `grep -n "3E6B8C" src/app/tokens.css` matches (portal hue mirrored to CSS)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The tool palette shows seven tools with Select default + amber-active; ShapeNode renders all four primitives composed through the background transform and persists via updateMap; ZoneLabel renders user text as Konva Text only; the style popover applies a preset + fill toggle + label; tokens.css mirrors the new hexes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire drawing into MapView + shapes persistence test + draw E2E</name>
  <files>src/features/person-map/MapView.tsx, tests/features/shapes.test.ts, e2e/draw-shapes.spec.ts</files>
  <read_first>
    - src/features/person-map/MapView.tsx (as modified by 03-02 — the three-layer structure, the Stage event wiring, the content layer L1 where ShapeNodes render)
    - src/features/person-map/editor/useToolMode.ts (Task 1) + ToolPalette/ShapeNode/StylePopover (Task 2)
    - src/features/person-map/coords.ts (stageToImage for capturing draw points)
    - src/db/repository.ts (updateMap)
    - e2e/map-create.spec.ts + e2e/marker.spec.ts (Playwright harness + window.__rb bridge for seeding a map)
    - .planning/phases/03-map-editor-spaces-navigation/03-RESEARCH.md (Pattern 2 commit-on-pointerup; Pattern 3 content-layer render order l.314-320)
    - .planning/phases/03-map-editor-spaces-navigation/03-UI-SPEC.md (Performance contract l.292-301 — keep culling; S2 canvas)
  </read_first>
  <action>
    In `src/features/person-map/MapView.tsx`: mount `ToolPalette` (driven by a `useToolMode` instance owned here). Route the Stage `onPointerDown/Move/Up` (and `onTouchStart/Move/End`) through the tool-mode handlers: in draw modes, capture the start point via `coords.stageToImage`, render a live preview, and on pointer-up commit the shape by appending to `map.shapes` and calling `updateMap(map.id, { shapes })`. Set `Stage.draggable` from `useToolMode.stageDraggable`. Render the active map's `shapes` as `ShapeNode` children in the L1 content layer (still culled via `useViewportCulling`, still ordered correctly — full layer ordering arrives in 03-05; for now render in array order). On shape single-click, select it and open the `StylePopover`. Keep two-finger pan/pinch working (wire the two-finger seam + `stage.stopDrag()` from Task 1). Do NOT regress the marker rendering or the empty-state.

    Create `tests/features/shapes.test.ts` (MAP-02): seed a map; simulate committing a rect shape descriptor (call the same `updateMap(map.id, { shapes: [...] })` path the UI uses) and assert it persists on `MapDoc.shapes`; assert a shape with a non-empty `label` is a valid zone (D-02); assert shape geometry stored is image-space and composes through `imageToStage`; assert updating a shape's preset/fill via `updateMap` round-trips.

    Create `e2e/draw-shapes.spec.ts` (MAP-02): seed a map via `window.__rb`; open the app, pick the Rect tool from the ToolPalette, drag on the canvas to draw a rectangle, and assert a shape now exists on the map (read back via the bridge / assert the canvas object count); pick a preset in the StylePopover and assert it persists. Mirror the existing E2E harness.
  </action>
  <verify>
    <automated>npx vitest run tests/features/shapes.test.ts --no-file-parallelism</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/shapes.test.ts --no-file-parallelism` passes (shape persists, zone = labeled shape, image-space geometry, preset/fill round-trip)
    - `grep -n "ToolPalette" src/features/person-map/MapView.tsx` matches and `grep -n "ShapeNode" src/features/person-map/MapView.tsx` matches (palette + shapes wired)
    - `grep -n "stageDraggable" src/features/person-map/MapView.tsx` matches (Stage draggable driven by tool mode)
    - `e2e/draw-shapes.spec.ts` exists, structured like the existing specs; runnable via `npx playwright test e2e/draw-shapes.spec.ts`
    - `npx tsc --noEmit` exits 0; existing marker/map E2E + unit tests still pass
  </acceptance_criteria>
  <done>MapView routes pointer events through the tool-mode machine to draw + commit shapes via updateMap, renders shapes (culled) with selection opening the style popover, keeps two-finger pan/pinch; shapes persist (unit) and an E2E proves drawing + styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered zone label → canvas Text | Untrusted text crosses into a rendered Konva object |
| user-drawn geometry → stored MapDoc.shapes | Untrusted geometry (vertex arrays, coords) crosses into storage + render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Zone labels rendered on canvas (could exfiltrate Drive token via XSS) | mitigate | ALL user text renders as Konva `Text`/React children — NEVER `dangerouslySetInnerHTML`; ZoneLabel test asserts no innerHTML usage (CONTEXT.md mandate) |
| T-03-11 | Tampering / DoS | Oversized polygon vertex arrays / NaN coords in MapDoc.shapes from cloud/backup | mitigate | ShapeSchema validates on load (03-01); MapView/ShapeNode tolerate absent/empty geometry; a size threshold rejects degenerate draws |
| T-03-SC | Tampering | npm installs (@radix-ui/react-popover optional) | mitigate | RESEARCH Package Legitimacy Audit clears @radix-ui/react-popover OK (50.9M dl/wk, canonical repo, no postinstall, same line as installed siblings); prefer the installed react-dialog if avoiding any new dep |
</threat_model>

<verification>
- `npx tsc --noEmit` green
- `npx vitest run tests/features --no-file-parallelism` green (useToolMode, shapes, coords, hierarchy)
- `npx playwright test e2e/draw-shapes.spec.ts` passes
- Manual: pick Rect, drag to draw a room, style it Stone with fill on, add label "Lobby" — the chip renders; reload and the shape + label persist
</verification>

<success_criteria>
- Tool palette with Select default + six other tools; amber marks the active tool
- Draw state machine: draw modes suppress single-pointer pan, two-finger always pans/pinches
- Rect/Ellipse/Line/Polygon draw and persist as MapDoc.shapes (image-space)
- Style popover applies a preset + fill toggle + zone label; labels render XSS-safe
- Shapes unit-tested + drawing E2E-covered; tokens mirrored to tokens.css
</success_criteria>

<output>
Create `.planning/phases/03-map-editor-spaces-navigation/03-03-SUMMARY.md` when done.
</output>

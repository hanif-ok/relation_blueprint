---
phase: 07-relationships-map-visual-polish
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/features/graph/GraphView.tsx
  - e2e/graph.spec.ts
  - src/features/person-map/editor/ConnectorLayer.tsx
  - src/features/person-map/connectors.ts
autonomous: true
requirements: [POL-01, POL-02, POL-03]
user_setup: []

must_haves:
  truths:
    - "The partial-cache newcomer-placement effect is fenced by suspendSaveRef — its first check after the cy null-check mirrors the layoutstop guard, so it never persists positions during an active ego-focus session (POL-03, D-12, Pitfall 1, WR-01)"
    - "A concurrent entity-add during an active focus session leaves the graphPositions meta row equal to the pre-focus base positions of the pre-existing nodes — NOT the transient concentric snapshot — proven by the new concurrent-mutation regression e2e spec (POL-03, D-12)"
    - "A newcomer added DURING a focus session is still auto-placed once focus exits (present in graphPositions, non-origin), via a fence-lifted re-trigger that fires in BOTH the synchronous and the async exit paths (POL-02, POL-03, D-08)"
    - "Newcomer placement persists through exactly ONE path — the fenced layoutstop handler; the redundant explicit savePositions is removed (IN-01) (POL-02)"
    - "A custom connector line color renders at 0.55 resting alpha, matching the default translucent hairline weight (POL-01, D-04, D-06, WR-02)"
    - "The map connector surface carries no unreachable relationship-selection code after WR-03 removal (POL-01)"
  prohibitions:
    - "The graph view NEVER writes db.people / db.groups / db.relationshipLinks — dragging, newcomer placement, and ego focus rearrange layout only, writing ONLY the graphPositions meta row (viewer-only, PROJECT.md, unchanged)"
    - "No unreachable connector `selected` / selectedRelationshipId code remains after WR-03 — the map connector surface exposes no dead selection capability"
    - "NO Dexie db.version() bump and NO src/db/schema.ts change; zero new dependencies (package.json unchanged)"
  artifacts:
    - path: "src/features/graph/GraphView.tsx"
      provides: "suspendSaveRef-fenced newcomer-placement effect + a fence-lifted re-place trigger; IN-01 single-persistence collapse"
      contains: "suspendSaveRef"
    - path: "e2e/graph.spec.ts"
      provides: "concurrent-mutation regression spec (add entity mid-focus → exit → base preserved + newcomer placed)"
    - path: "src/features/person-map/editor/ConnectorLayer.tsx"
      provides: "custom connector color at 0.55 render-time alpha; selection prop + amber branch removed"
      contains: "hexToRgba(connectorColor, 0.55)"
    - path: "src/features/person-map/connectors.ts"
      provides: "buildConnectors with the selectedRelationshipId option + Connector.selected field removed"
  key_links:
    - from: "src/features/graph/GraphView.tsx (newcomer-placement effect)"
      to: "suspendSaveRef"
      via: "first-check fence mirrors the layoutstop guard — no save during an ego-focus session"
      pattern: "suspendSaveRef\\.current"
    - from: "src/features/graph/GraphView.tsx (concentric-overlay exit branches)"
      to: "the placement effect"
      via: "a fence-lifted state tick bumped after suspendSaveRef.current=false in BOTH exit paths re-runs placement"
      pattern: "setPosCache|useState"
    - from: "src/features/person-map/editor/ConnectorLayer.tsx"
      to: "src/features/common/color.ts"
      via: "hexToRgba(connectorColor, 0.55) applies the resting alpha at render time"
      pattern: "hexToRgba\\(connectorColor"
---

<objective>
Close the three verified Phase-07 gaps from 07-VERIFICATION.md (`status: gaps_found`, 10/12) and 07-REVIEW.md, WITHOUT touching the four already-shipped plans (07-01..07-04):

- **WR-01 (blocking, truth #10 FAILED):** the partial-cache "place only the newcomer" effect in `GraphView.tsx` (~lines 323-337) calls `savePositions(cy)` with NO `suspendSaveRef` guard. If an entity is added while an ego-focus session is active (a background Drive sync PULL — the app's core reactive path — or the curator adding a person on another surface), the effect locks the CURRENTLY-DISPLAYED concentric positions and persists that transient snapshot to the durable `graphPositions` meta row, silently corrupting the saved base. In-session Exit-focus still looks correct (restores from in-memory `basePosRef`), so the corruption only surfaces on a LATER reopen.
- **WR-02 (consistency):** a custom connector color renders fully opaque while the default hairline is 55% alpha; the documented "alpha applied at render time, never baked" contract is violated.
- **WR-03 (unreachable code):** `selectedRelationshipId` is threaded into `ConnectorLayer`/`buildConnectors` but MapView (the only consumer, rendering inside `<Layer listening={false}>`) never passes it, so the amber-selected branch is dead. RESOLUTION = REMOVE the dead wiring (do NOT invent a new map-side relationship-selection UI — that is out of Phase-07 scope).

Purpose: restore the POL-03 "ego focus NEVER overwrites the persisted base" guarantee (D-12, the plan's own "single biggest landmine") on the concurrent-mutation path, plus two localized POL-01 connector-appearance corrections.
Output: a fenced + re-triggerable placement effect with a single persistence path, a failing-then-passing concurrent-mutation regression spec, and a coherent connector-appearance cleanup.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase gap sources (authoritative)
@.planning/phases/07-relationships-map-visual-polish/07-VERIFICATION.md
@.planning/phases/07-relationships-map-visual-polish/07-REVIEW.md
@.planning/phases/07-relationships-map-visual-polish/07-CONTEXT.md
@.planning/phases/07-relationships-map-visual-polish/07-VALIDATION.md

# Files this plan edits / references
@src/features/graph/GraphView.tsx
@src/features/graph/positionCache.ts
@src/features/person-map/editor/ConnectorLayer.tsx
@src/features/person-map/connectors.ts
@e2e/graph.spec.ts
</context>

<artifacts_produced>
Changed symbols / surfaces this plan produces:
- `GraphView.tsx` — the newcomer-placement effect (~lines 323-337) gains a `suspendSaveRef` first-check fence; a NEW `useState` "fence-lifted" tick (suggested name `postFocusPlaceTick`) is added and bumped in BOTH concentric-overlay exit branches after `suspendSaveRef.current = false`; the effect's dep array becomes `[partition, postFocusPlaceTick]`; the explicit `savePositions(cy).then(loadPositions…)` in the placement effect is REMOVED (IN-01 — persistence now flows only through the already-fenced `layoutstop` handler).
- `e2e/graph.spec.ts` — a NEW spec (suggested title "ego focus + concurrent entity-add: exit keeps the base and places the newcomer (WR-01 fence)").
- `ConnectorLayer.tsx` — custom color now renders via `hexToRgba(connectorColor, 0.55)`; **REMOVES** the `selectedRelationshipId` prop, the `selected` destructure, and the `selected ? colors.amber : …` / `selected ? 2.5 : 1.75` branches (a DELETION, not an addition).
- `connectors.ts` — **REMOVES** `selectedRelationshipId` from `BuildConnectorsOptions` and `selected` from the `Connector` interface + the pushed object (a DELETION).
No new files, no new exports, no new dependencies, no schema change.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Fence the newcomer-placement effect + re-place after focus exit + collapse double-persistence (GraphView)</name>
  <files>src/features/graph/GraphView.tsx</files>
  <read_first>
    - src/features/graph/GraphView.tsx (WHOLE file). Anchor identifiers: the `suspendSaveRef` ref (declared ~line 98); the concentric-overlay effect (~lines 243-315) — note its EXIT branch has TWO paths: a SYNCHRONOUS path when `basePosRef.current` is present (~lines 294-302, sets `suspendSaveRef.current = false` inline) and an ASYNC fallback when `basePosRef.current` is null (~lines 303-311, sets `suspendSaveRef.current = false` INSIDE a `loadPositions().then(...)` callback); the newcomer-placement effect (~lines 323-337, keyed on `[partition]`, calls `savePositions(cy)` at ~line 334 with NO fence check); the once-attached `layoutstop` handler (~line 367-380) which ALREADY has `if (suspendSaveRef.current) return;` as its first line and runs the `savePositions → loadPositions → setPosCache` chain; the `placedMissingRef` bookkeeping ref (~line 109).
    - .planning/phases/07-relationships-map-visual-polish/07-REVIEW.md § WR-01 (the exact fix pattern) + § IN-01 (double-persistence collapse)
    - .planning/phases/07-relationships-map-visual-polish/07-VERIFICATION.md frontmatter `gaps:` → `missing:` (the three required sub-fixes)
    - src/features/graph/positionCache.ts (savePositions/loadPositions/partitionCached — the persistence chain the placement effect and layoutstop share)
  </read_first>
  <action>
    Apply three coherent edits to the newcomer-placement effect and the concentric-overlay exit branches in GraphView.tsx:

    1. FENCE THE PLACEMENT EFFECT. Add `if (suspendSaveRef.current) return;` as the FIRST statement immediately after the existing `const cy = cyRef.current; if (!cy) return;` null-check, i.e. BEFORE the `if (partition.noneCached || partition.missing.length === 0) return;` early-return and BEFORE `placedMissingRef.current` is assigned. This mirrors the guard already present in the `layoutstop` handler. Because the guard returns before the `placedMissingRef.current = sig` assignment, a newcomer that arrives mid-focus is NOT recorded as placed — so it will still be placed after the fence lifts. Do NOT set `placedMissingRef.current` on the fenced bail.

    2. RE-PLACE AFTER EXIT (chosen mechanism — state it in the SUMMARY): add a new `useState` counter (suggested `const [postFocusPlaceTick, setPostFocusPlaceTick] = useState(0)`) that acts as a "fence-lifted" re-trigger, and add it to the placement effect's dependency array so the array becomes `[partition, postFocusPlaceTick]`. In the concentric-overlay effect's EXIT branch, call `setPostFocusPlaceTick((t) => t + 1)` immediately AFTER `suspendSaveRef.current = false` in BOTH exit paths: once in the synchronous `basePosRef.current` path, and once INSIDE the `loadPositions().then(...)` callback of the async fallback. This single mechanism guarantees the placement effect re-runs after the fence is actually cleared in both paths — do NOT rely on React effect-declaration ordering, and do NOT add `focusedId` to the placement effect deps (the tick is the deterministic trigger; adding focusedId would only re-run the fenced effect on enter for no benefit). Rationale to record: in the async path a synchronously-re-running placement effect would still observe `suspendSaveRef.current === true` and skip placement — the tick bump inside the promise callback is what re-checks placement once the async restore has cleared the fence.

    3. COLLAPSE DOUBLE-PERSISTENCE (IN-01). REMOVE the explicit `void savePositions(cy).then(() => loadPositions().then((positions) => setPosCache({ probed: true, positions })))` block from the END of the placement effect. The effect's `cy.layout({ name: 'cose', animate: false, fit: false, randomize: false }).run()` already emits `layoutstop`, whose once-registered handler runs the identical `savePositions → loadPositions → setPosCache` chain — and that handler is fenced by `suspendSaveRef`. Since the placement effect only proceeds when `suspendSaveRef.current` is false (the new guard), and the fence does not change during the synchronous effect body, the layoutstop emitted by this cose persists + refreshes the cache exactly once. After a post-exit re-placement the fence is already lifted (the tick bumps only after `suspendSaveRef.current = false`), so that cose's layoutstop is unfenced and persists the newcomer exactly once. Keep the `cy.batch(... .lock())` → `cy.layout(...).run()` → `cy.nodes().unlock()` sequence intact; only the trailing explicit save is removed.

    Update the effect's header comment (currently ~lines 317-322) to state that the effect is fenced by `suspendSaveRef` (never persists during an ego-focus session), that persistence flows solely through the fenced layoutstop handler, and that a fence-lifted tick re-runs placement after focus exits so a mid-focus newcomer is still placed. Do NOT change the `dragfree` handler, the `layoutstop` handler body, the concentric layout parameters, or any entity-table access (there is none — this stays viewer-only, writing ONLY the graphPositions meta row).
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p tsconfig.json --noEmit` exits 0 (clean).
    - In GraphView.tsx the newcomer-placement effect's FIRST statement after the `if (!cy) return;` null-check is `if (suspendSaveRef.current) return;` (source assertion), placed before the `partition.noneCached || partition.missing.length === 0` early-return and before any `placedMissingRef.current =` assignment.
    - The placement effect no longer contains a `savePositions(` call (the explicit save is removed; IN-01) — persistence is via the `layoutstop` handler only. `rg -n "savePositions" src/features/graph/GraphView.tsx` shows `savePositions` used only inside the `dragfree` and `layoutstop` handlers, not inside the `[partition, …]` placement effect.
    - A `useState` "fence-lifted" tick is added and appears in the placement effect's dependency array; its setter is called in BOTH concentric-overlay exit paths immediately after `suspendSaveRef.current = false` (the synchronous `basePosRef` path AND inside the async `loadPositions().then(...)` callback).
    - `git diff --exit-code src/db/schema.ts` is empty (no schema change); `git diff --exit-code package.json` is empty (no new deps).
    - `db.people` / `db.groups` / `db.relationshipLinks` are NOT referenced by the placement effect (viewer-only write boundary preserved).
  </acceptance_criteria>
  <done>The newcomer-placement effect honors the ego-focus fence (mirroring layoutstop), persists through a single fenced path, and a fence-lifted tick re-places any mid-focus newcomer once focus exits — in both the sync and async restore paths — so the durable base can never be clobbered by a concurrent mutation during focus.</done>
</task>

<task type="auto">
  <name>Task 2: Concurrent-mutation regression spec — add entity mid-focus, exit, assert base preserved + newcomer placed (graph.spec.ts)</name>
  <files>e2e/graph.spec.ts</files>
  <read_first>
    - e2e/graph.spec.ts (WHOLE file). Reuse harness: `seedGraph(page)` (seeds Alice/Bob/Team + links, returns ids), the `view-graph` nav click, the `__cyGraph` waitForFunction + `getElementById(id).emit('tap')` drive, the `graph-exit-focus` testid, and the `db.meta.get('graphPositions')` read pattern from the existing "ego focus is transient" spec (~lines 320-400) and the "sticky partial cache" spec (~lines 231-310, for the createPerson + createRelationshipLink newcomer pattern and the non-origin assertion).
    - .planning/phases/07-relationships-map-visual-polish/07-VERIFICATION.md frontmatter `gaps:` → `missing:` third bullet (the e2e obligation) + Gaps Summary (why the existing ego-transient spec misses the concurrent-mutation path)
    - .planning/phases/07-relationships-map-visual-polish/07-VALIDATION.md § "Per-Task Verification Map" + "Sampling Rate" (this is the new Dimension-8 validation obligation)
    - Project memory note: window.__rb / window.__cyGraph require e2e build mode (testbridge-requires-e2e-build-mode) — the suite already runs under real build + preview.
  </read_first>
  <action>
    Add ONE new Playwright spec to e2e/graph.spec.ts (suggested title "ego focus + concurrent entity-add: exit keeps the base and places the newcomer (WR-01 fence)"). Reuse `seedGraph` and the existing `__cyGraph` / `__rb.db` drive patterns; add no new fixtures. Sequence:

    1. Seed the graph and open the Graph view. Wait until `__cyGraph` exists and the initial `cose` has settled AND persisted a `graphPositions` row (poll `db.meta.get('graphPositions')` for a defined value), exactly as the existing ego-transient spec does — this is the pre-focus baseline.
    2. Snapshot the PRE-EXISTING nodes' persisted base: read `db.meta.get('graphPositions')` and capture the positions for Alice/Bob/Team (the base). Also capture the in-cy positions for cross-checking.
    3. ENTER ego focus by emitting `tap` on a node (e.g. Bob) so `focusedId` is set and the concentric overlay runs (the auto-save is now fenced). Optionally assert Bob's ProfileSidebar opens (AT bridge preserved).
    4. CONCURRENT MUTATION mid-focus: while still focused, add a NEW connected person (Carol) plus a relationship-link from Carol to an existing node (Alice) via `window.__rb.createPerson` + `window.__rb.createRelationshipLink` — mirroring the sticky-partial-cache spec. This changes `partition` mid-session and, pre-fix, triggers the unfenced placement save.
    5. EXIT focus: click `getByTestId('graph-exit-focus')`.
    6. ASSERT (a) — base preserved: after exit, `db.meta.get('graphPositions')` values for Alice/Bob/Team EQUAL the pre-focus base captured in step 2 (NOT the transient concentric snapshot). Use an exact/near-exact comparison consistent with the existing specs' rounding tolerance.
    7. ASSERT (b) — newcomer eventually placed: poll until the `graphPositions` row contains Carol's id with a non-origin position (`Math.abs(x) + Math.abs(y) > 1`), proving the fence-lifted re-trigger placed her after exit.

    This spec MUST FAIL against the pre-Task-1 code (existing-node positions in `graphPositions` become the concentric snapshot) and PASS after Task 1. Keep the existing five specs unchanged and green.
  </action>
  <verify>
    <automated>npx playwright test e2e/graph.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/graph.spec.ts --reporter=list` passes, including the NEW concurrent-mutation spec, under e2e build mode (build + preview; testbridge-requires-e2e-build-mode). All previously-passing specs (grabbable-flip, drag-persist, reset-layout, sticky-partial-cache, ego-transient) still pass.
    - The new spec adds an entity while `focusedId` is set (concurrent mutation during focus), then exits focus.
    - Assertion (a): post-exit `graphPositions` for the PRE-EXISTING nodes equals the pre-focus base (proves the concentric snapshot was NOT persisted).
    - Assertion (b): the newcomer appears in `graphPositions` with a non-origin position after exit (proves the fence-lifted re-place ran).
    - The spec reuses `seedGraph` / `__cyGraph` / `__rb` patterns; no new dependency, no new fixture file. `git diff --exit-code package.json` empty.
  </acceptance_criteria>
  <done>A regression spec locks the WR-01 fix: a data mutation during an ego-focus session can no longer corrupt the durable base, and a newcomer added mid-focus is still placed after exit. The spec is red pre-fix, green post-fix.</done>
</task>

<task type="auto">
  <name>Task 3: Custom connector color at 0.55 alpha (WR-02) + remove unreachable connector-selection wiring (WR-03)</name>
  <files>src/features/person-map/editor/ConnectorLayer.tsx, src/features/person-map/connectors.ts</files>
  <read_first>
    - src/features/person-map/editor/ConnectorLayer.tsx (WHOLE file). Anchor identifiers: `CONNECTOR_HAIRLINE = hexToRgba(colors.hairline, 0.55)` (~line 30); the `ConnectorLayerProps` interface incl. the selection prop (~lines 73-74) and the `connectorColor` doc (~lines 77-83); the destructured props incl. the selection default (~lines 86-94); the `buildConnectors(..., { …, dragOverride })` call (~lines 95-98); the `.map(({ id, a, b, directed, selected, label }) => …)` render (~line 102) and the `lineHex` / `lineStroke` / `topWidth` expressions (~lines 107-109); the top-of-file block comment (~lines 9-14) that references the amber-when-selected behavior.
    - src/features/person-map/connectors.ts (WHOLE file). Anchor identifiers: the `Connector` interface `selected: boolean` field (~lines 29-30); `BuildConnectorsOptions.selectedRelationshipId` (~lines 46-48); the `const { selectedRelationshipId = null, dragOverride = null } = opts;` destructure (~line 66); the `selected: selectedRelationshipId === link.id,` on the pushed connector (~line 111).
    - src/features/person-map/MapView.tsx (~lines 856-865) — the ONLY consumer; it renders `<ConnectorLayer>` inside `<Layer listening={false}>` and NEVER passes a selection prop (confirmed).
    - src/features/common/color.ts (the `hexToRgba` + `outlineColorFor` helpers, to confirm signatures)
    - .planning/phases/07-relationships-map-visual-polish/07-REVIEW.md § WR-02 (the exact alpha fix) + § WR-03 (removal rationale)
    <!-- planner-discipline-allow: selectedRelationshipId -->
  </read_first>
  <action>
    Two coherent edits to the SAME connector render expression, plus the pure-geometry cleanup. Do BOTH so no dangling `selected` reference is left.

    WR-02 (render-time alpha for a custom color): the resting connector line must apply the same 0.55 alpha the default `CONNECTOR_HAIRLINE` uses, so a custom color does not paint heavier/opaquer than the default aesthetic (mapAppearance.ts contract: the stored `#rrggbb` is raw and alpha is applied at render time, never baked). After the WR-03 removal below, the resting-line expression becomes exactly `const lineStroke = connectorColor ? hexToRgba(connectorColor, 0.55) : CONNECTOR_HAIRLINE;` using the existing `hexToRgba` helper. The casing basis stays a SOLID hex: `const lineHex = connectorColor ?? colors.hairline;` (so `outlineColorFor(lineHex)` can parse it), and `const casing = hexToRgba(outlineColorFor(lineHex), 0.6)` is unchanged.

    WR-03 (remove the unreachable relationship-selection wiring — REMOVE, do NOT wire a new selection UI). First VERIFY no live consumer depends on the removed field: grep every caller of `buildConnectors` and every reader of the connector `selected` field / the selection option across `src/` and `tests/` (already confirmed by the orchestrator: MapView is ConnectorLayer's only consumer, renders it non-interactive, and no test references the selection option/field — but re-verify before deleting). Then, keeping the change surgical (do NOT touch `selectedMarkerId` or any marker-selection path):
      - In `connectors.ts`: remove the `selected: boolean` field from the `Connector` interface, remove the selection option from `BuildConnectorsOptions`, remove it from the `const { … } = opts;` destructure (leaving `const { dragOverride = null } = opts;`), and remove the `selected: … === link.id,` line from the pushed connector object.
      - In `ConnectorLayer.tsx`: remove the selection prop from `ConnectorLayerProps`, remove it from the destructured props + default, remove it from the `buildConnectors(...)` options object (leaving `{ dragOverride }`), remove `selected` from the `.map(({ id, a, b, directed, selected, label }) => …)` destructure, and collapse the render expressions to drop every `selected`-driven branch: `lineHex` → `connectorColor ?? colors.hairline`; `lineStroke` → the WR-02 expression above; `topWidth` → the constant `1.75` (the former `selected ? 2.5 : 1.75`). Update the top-of-file block comment and the `connectorColor` prop doc so NO reference to amber-on-selection / the selection prop remains (remove those comment clauses too — the negative grep below must return zero including comments).
    If, contrary to the confirmed grounding, the grep finds a legitimate live consumer of the `selected` field on another surface, KEEP the field and remove ONLY the map-side dead wiring (prop + amber branch); note the deviation in the SUMMARY. Otherwise remove the field entirely. Update or remove any test that references the selection option/field so the suite stays green (grep confirmed none exist — expect no test edit).
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit && npx vitest run tests/features/connectors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -p tsconfig.json --noEmit` exits 0 (clean, no dangling `selected` reference).
    - `npx vitest run tests/features/connectors.test.ts` passes (the pure `buildConnectors` geometry suite stays green after the option/field removal).
    - The resting connector line stroke expression is `connectorColor ? hexToRgba(connectorColor, 0.55) : CONNECTOR_HAIRLINE` (source assertion) — a custom color renders at 0.55 alpha, matching the default hairline weight.
    - `rg -n selectedRelationshipId src/features/person-map` returns NO matches (the dead selection wiring — including comment references — is fully removed).
    - `rg -n "selected" src/features/person-map/connectors.ts` returns NO matches for the removed `Connector.selected` field / selection option (marker-selection paths in other files are untouched).
    - `topWidth` is the constant `1.75` (no `selected`-driven width); the casing underlay (`outlineColorFor(lineHex)` at 0.6 alpha) is preserved.
    - `git diff --exit-code package.json` empty; `git diff --exit-code src/db/schema.ts` empty.
  </acceptance_criteria>
  <done>A custom connector color renders at the documented 0.55 resting alpha (consistent with the default hairline), and the unreachable relationship-selection wiring is removed from both the pure geometry and the Konva layer — no dead capability, no dangling reference, marker selection untouched.</done>
</task>

</tasks>

<threat_model>
STRIDE-lite review at ASVS Level 1 (block_on: high). This is a local, viewer-only IndexedDB layout-position fix plus two connector-appearance corrections: NO new external input surface, NO auth, NO injection vector, NO network I/O, NO new dependency. The stored `connectorColor` still passes the existing `coerceHex` trust boundary (unchanged by this work). No new threats ≥ high were found.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| newcomer-placement effect → db.meta `graphPositions` | a layout-driven save that, unfenced during focus, could persist a transient concentric snapshot over the durable base (the WR-01 vector) |
| graph interaction (tap / drag / place / focus) → repository | viewer-only write boundary: only the `graphPositions` meta row may be written, NEVER db.people / db.groups / db.relationshipLinks |
| stored `connectorColor` hex → Konva render | `coerceHex`/`getMapAppearance` gate strips any non-`#rrggbb`; alpha applied at render time, never baked (unchanged this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | partial-cache placement effect → graphPositions save during an ego-focus session | mitigate | Add `if (suspendSaveRef.current) return;` as the effect's first check (mirrors layoutstop); the new concurrent-mutation e2e asserts `graphPositions` for pre-existing nodes equals the pre-focus base across an add-entity-mid-focus → exit cycle (Task 1 + Task 2). |
| T-07-04 | Tampering | graph view write boundary (place/drag/focus) | mitigate | Placement, drag, and ego focus write ONLY the `graphPositions` meta row; the existing viewer-only e2e (entity rows byte-identical) plus the new spec (base preserved) assert no entity-table mutation. Unchanged invariant, now regression-covered on the concurrent path. |
| T-07-01 | Tampering | stored `connectorColor` hex → canvas | accept | `coerceHex` gate (unchanged this plan) strips non-`#rrggbb`; WR-02 applies 0.55 alpha at render time, never baking it into the stored value. |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (native `hexToRgba`, built-in Cytoscape, existing test frameworks). `git diff --exit-code package.json` enforces. |

No high-severity threats — none block (block_on: high). The viewer-only write boundary is the security-relevant invariant this fix PRESERVES and the new regression test ASSERTS.
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` — clean (Tasks 1 & 3).
- `npx vitest run tests/features/connectors.test.ts tests/features/positionCache.test.ts` — green (pure geometry + partition suites unaffected by the option/field removal and the persistence collapse).
- `npm test` — full unit suite green (re-run `npx vitest run --no-file-parallelism` on fork-worker startup timeouts under load; that is environmental, not a code defect — vitest-forks-timeout-under-load).
- `npx playwright test e2e/graph.spec.ts --reporter=list` — all specs green INCLUDING the new concurrent-mutation spec (e2e build mode; testbridge-requires-e2e-build-mode). Confirm the new spec is RED against the pre-Task-1 code and GREEN after.
- `rg -n selectedRelationshipId src/features/person-map` — no matches (WR-03 dead wiring removed, comments included).
- `git diff --exit-code src/db/schema.ts` — empty (no Dexie version bump / no migration; schema-gate false-positives on Dexie, N/A here).
- `git diff --exit-code package.json` — empty (zero new dependencies).
- MANUAL UAT (07-VALIDATION.md, unchanged human items): rendered-pixel legibility on light/dark backgrounds + reduced-motion snap remain scoped manual — not re-opened by this fix.
</verification>

<success_criteria>
Ego focus can no longer corrupt the durable saved base when an entity is added mid-session: the newcomer-placement effect is fenced by `suspendSaveRef` (mirroring layoutstop), persists through a single fenced path, and a fence-lifted tick re-places any mid-focus newcomer once focus exits (both sync and async restore paths). A new regression spec proves the base is preserved and the newcomer is placed. Separately, a custom connector line renders at the documented 0.55 resting alpha, and the unreachable relationship-selection wiring is removed with no dangling reference and marker selection untouched. Closes WR-01 (truth #10 / POL-03, D-12, Pitfall 1), WR-02 (POL-01), and WR-03 (POL-01). No schema change, zero new dependencies, viewer-only write boundary preserved.
</success_criteria>

<output>
Create `.planning/phases/07-relationships-map-visual-polish/07-05-SUMMARY.md` when done.
</output>

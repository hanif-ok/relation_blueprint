---
phase: 07-relationships-map-visual-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/common/color.ts
  - src/features/person-map/mapAppearance.ts
  - tests/features/color.test.ts
  - tests/features/mapAppearance.test.ts
autonomous: true
requirements: [POL-01]
user_setup: []

must_haves:
  truths:
    - "A curator can pick ANY marker-label / connector colour and still read it on any map background — the pure derivations this plan proves (luminance-opposite halo + per-map persistence) are what make POL-01's on-canvas legibility guarantee possible; the user-visible rendered-pixel legibility itself is proven in Plan 02 (POL-01, D-04)"
    - "outlineColorFor returns the luminance-opposite neutral — a dark (slate) halo for a light label color, a light (paper) halo for a dark one — the non-negotiable D-04 legibility mechanism (POL-01, D-04)"
    - "A per-map colour choice persists in the single Dexie `meta` row `mapAppearance` keyed by map id, with NO schema migration and NO db.version() bump (POL-01, D-05)"
    - "A map with no stored appearance resolves to today's look — label = colors.paper, connector = default hairline — so existing databases render identically until customised (POL-01, D-06)"
    - "A malformed / non-#rrggbb stored hex coerces to the default rather than painting an undefined colour (POL-01, threat T-07-01, ASVS V5)"
  artifacts:
    - path: "src/features/common/color.ts"
      provides: "relativeLuminance(hex) + outlineColorFor(hex) pure helpers"
      exports: ["relativeLuminance", "outlineColorFor"]
    - path: "src/features/person-map/mapAppearance.ts"
      provides: "per-map colour persistence over db.meta 'mapAppearance'"
      exports: ["loadAppearance", "getMapAppearance", "setMapColor", "clearMapColor", "MapAppearance", "MapAppearanceRecord"]
    - path: "tests/features/color.test.ts"
      provides: "relativeLuminance + outlineColorFor unit coverage"
    - path: "tests/features/mapAppearance.test.ts"
      provides: "getMapAppearance default/stored/merge/clear + bad-hex coercion coverage"
  key_links:
    - from: "src/features/person-map/mapAppearance.ts"
      to: "src/db/schema.ts (db.meta)"
      via: "db.meta.get/put/transaction under key 'mapAppearance'"
      pattern: "mapAppearance"
    - from: "src/features/person-map/mapAppearance.ts"
      to: "src/features/common/color.ts"
      via: "imports colors defaults (colors.paper) for D-06 fallback"
      pattern: "colors\\.paper"
---

<objective>
Build the pure, unit-testable "brain" of POL-01: the luminance-derived outline helpers and the per-map colour persistence module. No canvas or DOM wiring here — this plan lands only functions and their tests so the colour derivation and the Dexie-meta read/merge/clear semantics are proven at the pure-function seam before any pixel is painted (Plan 02 consumes them).

Purpose: POL-01's legibility guarantee (D-04) and per-map persistence (D-05/D-06) are the load-bearing logic; the research (§ Validation Architecture) mandates pushing them into pure functions so the canvas-opaque behaviour is validated in Vitest, leaving only rendered-pixel contrast as manual UAT.
Output: `relativeLuminance` + `outlineColorFor` in `color.ts`; a new `mapAppearance.ts` module; two new unit test files, all green.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contracts (load-bearing for this plan)
@.planning/phases/07-relationships-map-visual-polish/07-CONTEXT.md
@.planning/phases/07-relationships-map-visual-polish/07-RESEARCH.md
@.planning/phases/07-relationships-map-visual-polish/07-PATTERNS.md
@.planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md
@.planning/phases/07-relationships-map-visual-polish/07-VALIDATION.md

# Analogs / source-of-truth for the patterns this plan replicates
@src/features/common/color.ts
@src/features/graph/positionCache.ts
@src/features/search/useScopeSelection.ts
@src/app/tokens.ts
@tests/features/positionCache.test.ts
</context>

<artifacts_produced>
New symbols this plan creates (for downstream executors / Plan 02):
- `relativeLuminance(hex: string): number` — WCAG sRGB→linear relative luminance (`src/features/common/color.ts`)
- `outlineColorFor(hex: string): string` — returns `colors.slate` for light fills, `colors.paper` for dark (luminance-opposite halo) (`src/features/common/color.ts`)
- `src/features/person-map/mapAppearance.ts` (NEW module) exporting:
  - `MapAppearance` type = `{ labelColor: string; connectorColor: string | null }`
  - `MapAppearanceRecord` type = `Record<string, { labelColor: string; connectorColor: string | null }>`
  - `loadAppearance(): Promise<MapAppearanceRecord>`
  - `getMapAppearance(record, mapId): MapAppearance` (PURE resolver; absent map → D-06 defaults `{ labelColor: colors.paper, connectorColor: null }`)
  - `setMapColor(mapId, field, hex): Promise<void>` (read-merge-put in one rw transaction)
  - `clearMapColor(mapId, field): Promise<void>` (delete the field → falls back to default)
- Dexie `meta` key `'mapAppearance'` (NO migration; rides the existing `meta` table)
- Test files: `tests/features/color.test.ts`, `tests/features/mapAppearance.test.ts`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relativeLuminance + outlineColorFor pure helpers in color.ts</name>
  <files>src/features/common/color.ts, tests/features/color.test.ts</files>
  <read_first>
    - src/features/common/color.ts (the existing `hexToRgba` — reuse its exact `replace('#','')` + `parseInt(h.slice(..),16)` parse shape)
    - src/app/tokens.ts (the `colors` object — `colors.slate` `#1B2230`, `colors.paper` `#F4F1EA`)
    - .planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md § "Legibility Contract" (the auto-luminance-opposite rule)
    - .planning/phases/07-relationships-map-visual-polish/07-RESEARCH.md § "relativeLuminance + outlineColorFor" (the WCAG formula + the safer contrast-ratio variant)
    - tests/features/positionCache.test.ts (the Vitest describe/it/expect precedent for a pure-function test file)
  </read_first>
  <behavior>
    - relativeLuminance('#FFFFFF') ≈ 1.0 (within 0.001)
    - relativeLuminance('#000000') === 0
    - relativeLuminance(colors.paper) is high (> 0.8); relativeLuminance(colors.slate) is low (< 0.05)
    - outlineColorFor('#FFFFFF') === colors.slate (light fill → dark halo)
    - outlineColorFor('#000000') === colors.paper (dark fill → light halo)
    - outlineColorFor(colors.paper) === colors.slate; outlineColorFor(colors.slate) === colors.paper
    - a mid-luminance colour (e.g. '#7A7A7A') returns whichever neutral has the higher WCAG contrast ratio (deterministic, never a same-luminance halo)
  </behavior>
  <action>
    Add two exported pure functions to `src/features/common/color.ts`, reusing the existing `hexToRgba` parse shape (`hex.replace('#','')` then `parseInt(slice,16)`).
    `relativeLuminance(hex)`: convert each channel to 0–1, apply the WCAG sRGB→linear transfer (channel/12.92 when ≤ 0.03928, else ((channel+0.055)/1.055)**2.4), return 0.2126*r + 0.7152*g + 0.0722*b.
    `outlineColorFor(hex)`: import `colors` from `@/app/tokens`; return the luminance-opposite neutral. Use the contrast-ratio variant (research-recommended, strictly safer): compute WCAG contrast (Lmax+0.05)/(Lmin+0.05) of the fill against `colors.slate` and against `colors.paper`, return whichever neutral yields the higher contrast (tie → `colors.slate`). This removes the 0.5-threshold assumption (research A1). Keep both functions dependency-free and side-effect-free.
    Write `tests/features/color.test.ts` covering every case in the behavior block (known-luminance values + the light→slate / dark→paper / mid→higher-contrast picks).
  </action>
  <verify>
    <automated>npx vitest run tests/features/color.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/color.test.ts` exits 0 with all cases green
    - `outlineColorFor('#FFFFFF') === '#1B2230'` (colors.slate) and `outlineColorFor('#000000') === '#F4F1EA'` (colors.paper)
    - `relativeLuminance` and `outlineColorFor` are exported from `src/features/common/color.ts` and are pure (no I/O, no mutation)
    - Existing `hexToRgba` export is unchanged (still imported by ConnectorLayer / graphStyle)
  </acceptance_criteria>
  <done>relativeLuminance + outlineColorFor exist, are pure, and their unit tests pass; the luminance-opposite halo mechanism is proven in Vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mapAppearance.ts per-map colour persistence over db.meta</name>
  <files>src/features/person-map/mapAppearance.ts, tests/features/mapAppearance.test.ts</files>
  <read_first>
    - src/features/graph/positionCache.ts (the single-row `meta` key/value save/load precedent)
    - src/features/search/useScopeSelection.ts (the PURE-resolver + read-merge-write-in-rw-transaction precedent — SCOPE_META_KEY, applyScopeChange, the db.transaction('rw', db.meta, …) wrapper)
    - src/db/schema.ts (the `meta` table + MetaRecord `{ key, value }` shape; confirm meta exists at version(1) — NO new version() needed)
    - src/app/tokens.ts (colors.paper for the D-06 label default)
    - .planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md § "Persistence Contract" (single `mapAppearance` row, shape Record<mapId,{labelColor,connectorColor}>, store solid #rrggbb)
    - .planning/phases/07-relationships-map-visual-polish/07-RESEARCH.md § "POL-01 … Persistence helper" + § "Security Domain" (coerce bad hex → default)
    - tests/features/positionCache.test.ts (the `beforeEach(() => db.meta.clear())` + async load/merge test precedent)
  </read_first>
  <behavior>
    - getMapAppearance({}, 'mapA') === { labelColor: colors.paper, connectorColor: null } (absent map → D-06 defaults)
    - getMapAppearance({ mapA: { labelColor: '#112233', connectorColor: '#445566' } }, 'mapA') returns those stored hexes
    - getMapAppearance with a malformed stored labelColor (e.g. 'red' or '#12') coerces that field to its default (labelColor → colors.paper; connectorColor → null) — never returns a non-#rrggbb value
    - after setMapColor('mapA','labelColor','#ABCDEF'), loadAppearance() then getMapAppearance(...,'mapA').labelColor === '#ABCDEF' and connectorColor stays null
    - setMapColor merges — setting connectorColor for mapA does not drop mapA.labelColor, and does not touch mapB
    - after clearMapColor('mapA','labelColor'), getMapAppearance(...,'mapA').labelColor falls back to colors.paper (field removed)
    - two rapid setMapColor calls each compose over the latest persisted record (rw-transaction serialisation, no lost write)
  </behavior>
  <action>
    Create `src/features/person-map/mapAppearance.ts` mirroring positionCache.ts + useScopeSelection.ts. Meta key constant `'mapAppearance'`. Types: `MapAppearance = { labelColor: string; connectorColor: string | null }` and `MapAppearanceRecord = Record<string, { labelColor: string; connectorColor: string | null }>`.
    `loadAppearance()`: read `db.meta.get('mapAppearance')`, return `(row?.value as MapAppearanceRecord) ?? {}`.
    `getMapAppearance(record, mapId)` — PURE: look up `record[mapId]`; return `{ labelColor: <valid #rrggbb or colors.paper>, connectorColor: <valid #rrggbb or null> }`. Validate each stored value against a `#rrggbb` shape (6-hex) and coerce a non-matching value to its default (D-06 default + threat T-07-01). Absent map → `{ labelColor: colors.paper, connectorColor: null }`.
    `setMapColor(mapId, field, hex)` where field is `'labelColor' | 'connectorColor'`: read-merge-put inside `db.transaction('rw', db.meta, …)` exactly like useScopeSelection.setFieldChecked — read the current record, shallow-merge `{ ...record, [mapId]: { ...(record[mapId] ?? default), [field]: hex } }`, put back. Store the solid `#rrggbb` straight from the picker — NEVER bake alpha (Pitfall 5; alpha is applied at render in Plan 02).
    `clearMapColor(mapId, field)`: same rw transaction; delete that field from `record[mapId]` (set to the sentinel: labelColor → remove so default applies; connectorColor → remove/null); if the map object becomes empty, drop the map key. Falls back to the D-06 default via getMapAppearance.
    Write `tests/features/mapAppearance.test.ts` using the `beforeEach(() => db.meta.clear())` precedent, covering every behavior-block case incl. bad-hex coercion and the merge/no-cross-map-clobber cases.
    Do NOT add a `db.version()` bump, a new table, or a MapDoc field — this rides the existing `meta` table ([[schema-gate-dexie-false-positive]]).
  </action>
  <verify>
    <automated>npx vitest run tests/features/mapAppearance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/features/mapAppearance.test.ts` exits 0 with all behavior-block cases green
    - `getMapAppearance({}, 'x')` deep-equals `{ labelColor: '#F4F1EA', connectorColor: null }`
    - A malformed stored hex never escapes getMapAppearance (coerced to default) — verified by an explicit test case
    - `src/db/schema.ts` has NO new `this.version(...)` call added (git diff of schema.ts is empty)
    - `git diff --exit-code package.json` passes (no new dependency)
  </acceptance_criteria>
  <done>mapAppearance.ts persists per-map colours on the existing meta row with pure default/merge/clear + bad-hex coercion, all unit-tested green; no schema migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tampered Dexie `meta` row → getMapAppearance | a `mapAppearance` value could be edited out-of-band to a non-`#rrggbb` string |
| stored hex → (Plan 02) Konva canvas paint | the resolved colour is later painted into a non-executable `<canvas>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | getMapAppearance (mapAppearance.ts) | mitigate | Validate each stored value against a `#rrggbb` shape; coerce a non-matching value to the D-06 default (unit-tested). A bad value can never reach the canvas. |
| T-07-03 | Tampering | `mapAppearance` meta row | accept | Appearance is a regenerable local convenience with no integrity requirement (device-local, unsynced, like graphPositions); worst case a wrong colour, fixed by re-picking / Reset. |
| T-07-SC | Tampering | npm installs | mitigate | This plan installs zero packages (native + existing libs). `git diff --exit-code package.json` in acceptance criteria enforces it. |

No high-severity threats (block_on: high) — none block.
</threat_model>

<verification>
- `npx vitest run tests/features/color.test.ts tests/features/mapAppearance.test.ts` — both green
- `git diff --exit-code package.json` — no new dependency (D-02 / UI-SPEC "New dependencies: none")
- `git diff` of `src/db/schema.ts` empty — NO migration (Dexie meta pattern, [[schema-gate-dexie-false-positive]])
</verification>

<success_criteria>
relativeLuminance/outlineColorFor and the mapAppearance module exist, are pure/typed, and their unit tests pass — POL-01's legibility derivation (D-04) and per-map persistence (D-05/D-06) are proven at the pure-function seam, ready for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/07-relationships-map-visual-polish/07-01-SUMMARY.md` when done.
</output>

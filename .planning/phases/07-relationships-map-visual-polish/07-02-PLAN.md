---
phase: 07-relationships-map-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/features/person-map/AvatarMarker.tsx
  - src/features/person-map/editor/ConnectorLayer.tsx
  - src/features/person-map/editor/LayersPanel.tsx
  - src/features/person-map/MapView.tsx
autonomous: true
requirements: [POL-01]
user_setup: []

must_haves:
  truths:
    - "The marker name label renders in the per-map label colour with a luminance-opposite halo (Konva Text stroke + fillAfterStrokeEnabled + shadow) so any chosen colour reads over light AND dark background images (POL-01, D-04)"
    - "The connector line renders in the per-map connector colour over a cased underlay arrow; a SELECTED connector still swaps its top line to amber while the casing stays (POL-01, D-04, amber-discipline A8)"
    - "Two native <input type=color> pickers live in the map LayersPanel Appearance block (map-only scope) and live-update the canvas on change; a per-row Reset restores the D-06 default (POL-01, D-01, D-02, D-03, D-06)"
    - "Defaults keep today's look — label defaults to colors.paper, connector to the existing CONNECTOR_HAIRLINE — so existing maps render identically until customised (POL-01, D-06)"
  artifacts:
    - path: "src/features/person-map/AvatarMarker.tsx"
      provides: "labelColor prop + luminance halo on the name-label Text"
      contains: "fillAfterStrokeEnabled"
    - path: "src/features/person-map/editor/ConnectorLayer.tsx"
      provides: "connectorColor prop + cased-line underlay Arrow"
      contains: "connectorColor"
    - path: "src/features/person-map/editor/LayersPanel.tsx"
      provides: "Appearance block with map-label-color / map-connector-color pickers + per-row Reset"
    - path: "src/features/person-map/MapView.tsx"
      provides: "useLiveQuery(loadAppearance) + getMapAppearance threading + setMapColor/clearMapColor writers"
  key_links:
    - from: "src/features/person-map/MapView.tsx"
      to: "src/features/person-map/mapAppearance.ts"
      via: "useLiveQuery(loadAppearance) → getMapAppearance(record, map.id)"
      pattern: "getMapAppearance"
    - from: "src/features/person-map/MapView.tsx"
      to: "src/features/person-map/AvatarMarker.tsx + editor/ConnectorLayer.tsx"
      via: "passes appearance.labelColor / appearance.connectorColor props"
      pattern: "labelColor|connectorColor"
    - from: "src/features/person-map/editor/LayersPanel.tsx"
      to: "src/features/person-map/mapAppearance.ts"
      via: "picker onChange → setMapColor(map.id, field, hex); Reset → clearMapColor"
      pattern: "setMapColor|clearMapColor"
---

<objective>
Wire Plan 01's helpers into the visible surface: paint the per-map label colour with a luminance halo on the Konva marker, paint the per-map connector colour over a cased underlay, and mount the two native colour pickers in the existing map LayersPanel with live persistence. This closes the Phase-04 white-on-white legibility gap (UAT tests 6 & 7) structurally — any chosen colour reads over any background image.

Purpose: deliver the user-facing half of POL-01. Plan 01 proved the derivation/persistence in Vitest; this plan threads those colours through AvatarMarker, ConnectorLayer, LayersPanel, and MapView per the UI-SPEC Legibility + Interaction contracts.
Output: customisable, halo-backed, per-map marker-label + connector colours with LayersPanel pickers and Reset.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contracts (load-bearing)
@.planning/phases/07-relationships-map-visual-polish/07-CONTEXT.md
@.planning/phases/07-relationships-map-visual-polish/07-RESEARCH.md
@.planning/phases/07-relationships-map-visual-polish/07-PATTERNS.md
@.planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md

# Consumed from Plan 01
@.planning/phases/07-relationships-map-visual-polish/07-01-SUMMARY.md
@src/features/common/color.ts
@src/features/person-map/mapAppearance.ts

# Files this plan edits (their own current code is the analog)
@src/features/person-map/AvatarMarker.tsx
@src/features/person-map/editor/ConnectorLayer.tsx
@src/features/person-map/editor/LayersPanel.tsx
@src/app/tokens.ts
</context>

<artifacts_produced>
New symbols / surfaces this plan creates:
- `AvatarMarkerProps.labelColor?: string` (default `colors.paper`) — threaded to the name-label `Text.fill` + halo
- `ConnectorLayerProps.connectorColor?: string | null` — threaded to the resting connector line (null → `CONNECTOR_HAIRLINE`)
- LayersPanel props: `appearance: MapAppearance`, `onLabelColorChange`, `onConnectorColorChange`, `onResetLabelColor`, `onResetConnectorColor` (writers passed down from MapView)
- New e2e `data-testid`s: `map-label-color`, `map-connector-color`
- LayersPanel "Appearance" block with two colour rows + per-row Reset affordance
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Marker-label halo + connector casing (Konva render)</name>
  <files>src/features/person-map/AvatarMarker.tsx, src/features/person-map/editor/ConnectorLayer.tsx</files>
  <read_first>
    - src/features/person-map/AvatarMarker.tsx (the name-label Text at ~l.257–269, currently fill={colors.paper}; and the existing shadow'd Text/Circle above at ~l.240–252 — mirror those shadow prop names)
    - src/features/person-map/editor/ConnectorLayer.tsx (the Arrow at ~l.94–109; CONNECTOR_HAIRLINE at l.30; the existing selected→amber logic at l.95)
    - src/features/common/color.ts (outlineColorFor + hexToRgba — both used here)
    - .planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md § "Legibility Contract" (exact halo/casing values: strokeWidth 2, shadow black@0.55 blur 3 offsetY 1, casing +2px @0.6 alpha)
    - .planning/phases/07-relationships-map-visual-polish/07-RESEARCH.md § "Legibility Contract — verified Konva API" (fillAfterStrokeEnabled is the crux; VERIFIED konva 10.3)
    - .planning/phases/07-relationships-map-visual-polish/07-PATTERNS.md (AvatarMarker + ConnectorLayer pattern-apply rows)
  </read_first>
  <action>
    AvatarMarker: add an optional `labelColor?: string` prop (default `colors.paper` so D-06 is preserved). On the `showLabels && <Text>` name label, set `fill={labelColor}` and add the halo props: `stroke={outlineColorFor(labelColor)}`, `strokeWidth={2}`, `fillAfterStrokeEnabled` (the crux — fill paints over the stroke's inner half so the glyph keeps colour+weight), `lineJoin="round"`, `shadowColor="#000000"`, `shadowOpacity={0.55}`, `shadowBlur={3}`, `shadowOffsetY={1}`. Keep `listening={false}`. Import `outlineColorFor` from `@/features/common/color`. The label text still flows straight into the Konva `text` prop — never HTML (XSS boundary preserved).
    ConnectorLayer: add an optional `connectorColor?: string | null` prop (default `null` → the resting line stays `CONNECTOR_HAIRLINE`, D-06). Inside the existing `<Group listening={false}>` per connector, compute `line = selected ? colors.amber : (connectorColor ?? CONNECTOR_HAIRLINE)`. Render a FIRST underlay casing `Arrow` (same `points`, same `pointerLength`/`pointerWidth` arrowhead geometry, `strokeWidth` = the top line's width + 2, `stroke`/`fill` = `hexToRgba(outlineColorFor(line), 0.6)`), then the existing colored `Arrow` on top (unchanged widths 1.75 resting / 2.5 selected). Preserve `perfectDrawEnabled={false}` + `listening={false}` on BOTH arrows. Amber stays on the TOP line only when selected (A8); the casing remains under a selected line too.
    Store nothing here — colours arrive as props (threaded by MapView in Task 2). Do not import mapAppearance in these leaf components.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit && npx vitest run tests/features/color.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles (`npx tsc -p tsconfig.json --noEmit` exits 0)
    - `AvatarMarker.tsx` name-label Text uses `fill={labelColor}` and includes `fillAfterStrokeEnabled` + `stroke={outlineColorFor(...)}` (source assertion)
    - `ConnectorLayer.tsx` renders a casing Arrow at `strokeWidth` = line width + 2 with `hexToRgba(outlineColorFor(line), 0.6)`, beneath the colored Arrow; selected still overrides the top line to `colors.amber`
    - Default behaviour unchanged when props omitted: `labelColor` defaults to `colors.paper`, connector resting stays `CONNECTOR_HAIRLINE` (D-06)
    - `git diff --exit-code package.json` passes; `src/features/graph/graphStyle.ts` untouched (git diff empty)
  </acceptance_criteria>
  <done>The marker label paints in labelColor with a luminance-opposite halo; the connector paints in connectorColor over a cased underlay; amber-on-select preserved; defaults keep today's look.</done>
</task>

<task type="auto">
  <name>Task 2: LayersPanel Appearance pickers + MapView live threading</name>
  <files>src/features/person-map/editor/LayersPanel.tsx, src/features/person-map/MapView.tsx</files>
  <read_first>
    - src/features/person-map/editor/LayersPanel.tsx (the two existing toggle rows at ~l.255–276 — the Appearance block mounts right after; `map` (and `map.id`) already in scope; mirror the `data-testid` convention `show-labels-toggle`)
    - src/features/person-map/MapView.tsx (l.348–353 showLabels/showConnectorLabels useState; l.193 map live query; l.776–786 LayersPanel render; l.838–845 ConnectorLayer render; l.888–896 AvatarMarker render)
    - src/features/person-map/mapAppearance.ts (loadAppearance, getMapAppearance, setMapColor, clearMapColor — from Plan 01)
    - src/features/search/useScopeSelection.ts (the useLiveQuery(() => db.meta.get(...)) live-read precedent for continuous picker onChange)
    - .planning/phases/07-relationships-map-visual-polish/07-UI-SPEC.md § "Interaction Contracts IC-1" + § "Copywriting Contract" (labels "Name label color" / "Connector color"; Reset affordance; heading "Appearance") + § "Accessibility" (≥44px row, associated <label>)
  </read_first>
  <action>
    LayersPanel: after the two existing toggle rows, add an "Appearance" block (optional 13px/600 heading "Appearance") with two labelled rows. Row A `<label>Name label color</label>` + `<input type="color" data-testid="map-label-color">` whose `value` is `appearance.labelColor` and `onChange` calls `onLabelColorChange(e.target.value)`, plus a small "Reset" text button (aria-label "Reset to default color") calling `onResetLabelColor`. Row B `<label>Connector color</label>` + `<input type="color" data-testid="map-connector-color">` value `appearance.connectorColor ?? colors.hairline` (show the default swatch when unset), onChange `onConnectorColorChange(e.target.value)`, "Reset" → `onResetConnectorColor`. Wrap each row so its hit-area is ≥44px tall. Add the new props to `LayersPanelProps`: `appearance: MapAppearance`, `onLabelColorChange: (hex: string) => void`, `onConnectorColorChange: (hex: string) => void`, `onResetLabelColor: () => void`, `onResetConnectorColor: () => void`. Keep LayersPanel presentational — no db calls here.
    MapView: add `const appearanceRecord = useLiveQuery(() => loadAppearance(), []);` and `const appearance = getMapAppearance(appearanceRecord ?? {}, map?.id ?? '');` (guard when no active map). Pass `appearance` + the four writers to `<LayersPanel>`; each writer binds `map.id` — e.g. `onLabelColorChange={(hex) => void setMapColor(map.id, 'labelColor', hex)}`, `onResetLabelColor={() => void clearMapColor(map.id, 'labelColor')}` (and the connector pair). Thread `labelColor={appearance.labelColor}` onto every `<AvatarMarker>` (l.889 region) and `connectorColor={appearance.connectorColor}` onto `<ConnectorLayer>` (l.839 region). useLiveQuery re-renders on every `meta.put`, so dragging the native picker live-updates the canvas (IC-1). Colours are per-map, device-local, unsynced — do NOT route them through the repository/serializer/SyncEngine.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.json --noEmit && npx vitest run tests/features/mapAppearance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles (`npx tsc -p tsconfig.json --noEmit` exits 0)
    - LayersPanel renders `data-testid="map-label-color"` and `data-testid="map-connector-color"` native color inputs, each with an associated visible `<label>` and a Reset affordance (source assertion)
    - MapView reads appearance via `useLiveQuery(() => loadAppearance())` + `getMapAppearance(..., map.id)` and passes `labelColor`/`connectorColor` to AvatarMarker/ConnectorLayer and the four writers to LayersPanel (source assertion)
    - Picker onChange writes only the `mapAppearance` meta row (via setMapColor) — no db.people/maps/relationshipLinks write in the appearance path (viewer/appearance-only)
    - `git diff --exit-code package.json` passes; graphStyle.ts untouched
  </acceptance_criteria>
  <done>Two per-map colour pickers live in the LayersPanel Appearance block, persist through mapAppearance.ts, and live-update the halo-backed canvas; Reset restores D-06 defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native color picker (DOM) → mapAppearance meta → Konva paint | the sole new input this phase; a `#rrggbb` hex painted into a non-executable `<canvas>` |
| user entity text (person.name) → Konva Text | unchanged label-render boundary (must stay canvas Text, never HTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | color hex → Konva stroke/fill | mitigate | Hex is validated/coerced in getMapAppearance (Plan 01) before it reaches props; a `#rrggbb` is inert as a Konva color — no code execution. |
| T-07-02 | Tampering/Elevation | marker name label / connector label | mitigate | Labels remain Konva `Text` (`text` prop), never `dangerouslySetInnerHTML`; only colours are customised this phase — no injection surface added (T-04-01 preserved). |
| T-07-SC | Tampering | npm installs | mitigate | Native `<input type=color>` + existing Konva; zero installs. `git diff --exit-code package.json` enforces. |

No high-severity threats — none block (block_on: high).
</threat_model>

<verification>
- `npx tsc -p tsconfig.json --noEmit` — compiles
- `npm test` (`vitest run`) — full unit suite stays green (no regression; if fork-worker timeout under load, re-run `npx vitest run --no-file-parallelism` [[vitest-forks-timeout-under-load]])
- `git diff --exit-code package.json` — no new dependency
- `git diff` of `src/features/graph/graphStyle.ts` empty — graph stays token-driven (D-01)
- MANUAL UAT (VALIDATION.md, canvas-legibility is not headless-assertable): light-map light label reads via dark halo; dark-map dark label reads via light halo; connector casing reads on both — screenshots
</verification>

<success_criteria>
A curator can set a per-map marker-label colour and connector colour from the LayersPanel; any choice stays legible over light and dark background images via the auto halo/casing; defaults keep today's look; choices persist across reload. Closes the Phase-04 white-on-white gap.
</success_criteria>

<output>
Create `.planning/phases/07-relationships-map-visual-polish/07-02-SUMMARY.md` when done.
</output>

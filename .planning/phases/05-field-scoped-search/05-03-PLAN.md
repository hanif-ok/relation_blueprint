---
phase: 05-field-scoped-search
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/features/search/snippet.ts
  - src/features/search/SearchResultRow.tsx
  - src/features/search/searchIndex.ts
  - src/features/search/useSearchIndex.ts
  - tests/features/snippet.test.ts
  - tests/features/searchIncremental.test.ts
  - e2e/search-incremental.spec.ts
autonomous: true
requirements: [SRCH-01]
user_setup: []

must_haves:
  truths:
    - "A result matched on a NON-name field shows a matched-field snippet on the secondary line — the field label + a context window with the matched term wrapped in a <mark> (e.g. job: black[smith]) (D-09)"
    - "A result matched on the Name field falls back to the normal BrowseRow secondary line (tags, else updated Nd ago) — no redundant name snippet (D-09, B6)"
    - "When multiple fields match, the highest-boosted matched field's snippet is shown (name > tags/phone > description/notes > custom neutral) (D-07)"
    - "Creating/editing/deleting a person updates the search results incrementally via the repository change signal — the index is not fully rebuilt on every load (SRCH-01 criterion 3)"
    - "The matched term, snippet, field label, and entity name all render as React children — never injected HTML (T-05-01)"
  artifacts:
    - path: "src/features/search/snippet.ts"
      provides: "Pure highlight helper — picks the highest-boosted matched non-name field and returns React children with the term in a <mark>"
      min_lines: 30
    - path: "src/features/search/useSearchIndex.ts"
      provides: "Incremental index maintenance — subscribes repository.onChange and maps op→add/replace/discard"
  key_links:
    - from: "src/features/search/useSearchIndex.ts"
      to: "src/db/repository.ts"
      via: "onChange(ev => applyChange(...)) filtered to entityType people/fieldDefs, mapping op→add/replace/discard"
      pattern: "onChange"
    - from: "src/features/search/SearchResultRow.tsx"
      to: "src/features/search/snippet.ts"
      via: "renders the snippet React children for a non-name match from the hit's match metadata + fieldTextById"
      pattern: "snippet|buildSnippet"
  prohibitions:
    - "No full index rebuild on entity change — create/update/delete route through add/replace/discard (criterion 3)"
    - "No raw-HTML injection — the <mark> and every text fragment are real React elements/children"
    - "No Dexie migration / schema-push task — read-only phase"
    - "No re-introduction of a name-field snippet — name matches use the BrowseRow fallback line (B6)"
---

<objective>
Deliver the two remaining locked outcomes: the matched-field snippet (D-09) — the visible EVIDENCE that scoping works, showing which non-name field matched with the term highlighted (job: black[smith]) — and incremental index freshness (SRCH-01 criterion 3): the index updates through the repository change signal as people change, rather than rebuilding on every load. Together they make the smith-vs-blacksmith scoping trustworthy (you can see why each row remains) and fast toward thousands of records.

Purpose: Close the phase — turn the working scoped search into one the user can trust (snippet evidence, D-09) and that stays fast and live at scale (incremental updates, criterion 3), keeping the XSS boundary intact.
Output: A pure snippet helper + its test; the result row rendering the snippet; the index gaining a pure `applyChange` add/replace/discard path; the hook subscribing to `repository.onChange`; unit + e2e proofs of incremental freshness and safe highlighting.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-field-scoped-search/05-CONTEXT.md
@.planning/phases/05-field-scoped-search/05-PATTERNS.md
@.planning/phases/05-field-scoped-search/05-UI-SPEC.md
@.planning/phases/05-field-scoped-search/05-02-SUMMARY.md

# Reuse surfaces (verify current shapes before writing code)
@src/features/browse/BrowseRow.tsx
@src/db/repository.ts
@src/features/connect/useSyncEngine.ts
</context>

<artifacts_this_phase_produces>
Net-new symbols this plan (05-03) adds (exclude from source-grounding drift checks):

- `src/features/search/snippet.ts` — the pure highlight helper (`pickMatchedField` + `buildSnippet` returning React children) — no direct in-repo analog (novel but tiny).
- Extensions to `searchIndex.ts`: a pure `applyChange(index, fieldTextById, ev, people, customDefs)` add/replace/discard path.
- Extensions to `useSearchIndex.ts`: the `repository.onChange` subscription driving incremental updates (replacing plan-01's rebuild-on-change).
- Extensions to `SearchResultRow.tsx`: the matched-field snippet secondary line.
See 05-01-PLAN.md for the full phase artifact list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matched-field snippet — the evidence the scoping works (D-09, D-07)</name>
  <files>src/features/search/snippet.ts, tests/features/snippet.test.ts, src/features/search/SearchResultRow.tsx</files>
  <read_first>
    - `.planning/phases/05-field-scoped-search/05-PATTERNS.md` (§ snippet.ts + § SearchResultRow.tsx — the pure string→React-children highlight, the name-match fallback, the highest-boosted-field pick)
    - `src/features/search/searchIndex.ts` (plan-02 output — the exported boost map, the built-in key→label map, and the `fieldTextById` projection the snippet reads for the matched field's value; the `SearchHit.match` metadata shape)
    - `src/features/search/SearchResultRow.tsx` (plan-01 output — the secondary-line block being replaced for non-name matches; keep the name-fallback path)
    - `src/features/browse/BrowseRow.tsx` (the exact tags/`updatedAgo` fallback secondary line to keep for name matches, ~lines 107-119)
    - `.planning/phases/05-field-scoped-search/05-UI-SPEC.md` (§ Typography "Matched-field snippet", § Color amber usage 2 — the `<mark>` amber-tint highlight B1; the XSS boundary note)
  </read_first>
  <action>
    Create `src/features/search/snippet.ts` — a PURE module (React children in, no DOM/HTML strings):
    - `pickMatchedField(match, boostMap)` — from a MiniSearch `match` (term → matched field names), collect the matched field keys, EXCLUDE the name key, and return the highest-boosted remaining key (tags/phone > description/notes > custom neutral, per D-07). Return undefined when the only match is the name field (or nothing non-name matched) — the caller then uses the BrowseRow fallback line.
    - `buildSnippet(fieldLabel, valueText, terms, contextChars = 30)` — find the first matched term within `valueText` (case-insensitive), take a ~30-char context window either side (ellipsize when truncated), and return an array of React children: a `{fieldLabel}: ` label prefix, the leading context text, a real `<mark>` element wrapping the matched substring, and the trailing context text. Return only React elements/strings — never assemble an HTML string and never use the attribute that injects raw HTML.

    Update `SearchResultRow.tsx`: for a hit whose `pickMatchedField(hit.match, boostMap)` returns a non-name field key, render `buildSnippet(label, fieldTextById.get(person.id)?.[key], hit.terms)` on the secondary line (Label/13 ink-muted, `<mark>` amber-tint per B1). For a name-only match (or when `pickMatchedField` is undefined), keep the plan-01 BrowseRow fallback secondary line (tags chips, else `updatedAgo`). Add `data-testid="search-snippet"` to the snippet container and keep the `<mark>` a real element.
  </action>
  <behavior>
    - `pickMatchedField({ smith: ['builtin:notes','<jobId>'] }, boostMap)` returns `<jobId>` only if its boost ≥ notes; with equal/neutral custom vs notes weight, the tie-break is deterministic and documented (prefer the higher-boosted; custom neutral = 1, notes = 1 → pick by a stable order).
    - `pickMatchedField({ smith: ['builtin:name'] }, boostMap)` returns undefined (name-only → fallback line).
    - `pickMatchedField({ smith: ['builtin:name','<jobId>'] }, boostMap)` returns `<jobId>` (name excluded, the non-name field surfaces the evidence).
    - `buildSnippet('Job', 'blacksmith', ['smith'])` yields children whose text content is `Job: …black` + `<mark>smith</mark>` (the matched substring inside a mark), with the prefix and value as separate children.
    - A term at the start/end of the value produces no leading/trailing ellipsis on that side.
  </behavior>
  <acceptance_criteria>
    - `npx vitest run tests/features/snippet.test.ts` passes with the pick-field (name excluded / highest-boosted) and build-snippet (mark-wrapped substring, context window, ellipsis) assertions above. Assert the returned nodes are React elements (the mark is `type === 'mark'`), not strings of HTML.
    - The result row renders `<mark>` for a non-name match and the tags/`updatedAgo` fallback for a name match.
    - `grep -rn "dangerouslySetInnerHTML={" src/features/search/` returns no matches.
    - `npm run typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/features/snippet.test.ts && npm run typecheck</automated>
  </verify>
  <done>Non-name matches show which field matched with the term highlighted in a real `<mark>`; name matches keep the plain fallback line; the highest-boosted matched field wins; all fragments are React children.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Incremental index freshness — repository change signal drives add/replace/discard (SRCH-01 criterion 3)</name>
  <files>src/features/search/searchIndex.ts, src/features/search/useSearchIndex.ts, tests/features/searchIncremental.test.ts, e2e/search-incremental.spec.ts</files>
  <read_first>
    - `.planning/phases/05-field-scoped-search/05-PATTERNS.md` (§ useSearchIndex.ts — the `ChangeEvent`/`onChange` API, the op→add/replace/discard mapping, the people + fieldDefs filter, post-commit note)
    - `src/db/repository.ts` (~lines 37-52: `ChangeEvent { entityType, entityId, op }` + `onChange(listener): () => void`; `updatePerson` emits `op:'update'`; `deleteEntity` emits `op:'delete'` post-commit; `applyFieldTypeChange` emits per-entity updates post-commit; the `people`/`fieldDefs` entityType literals)
    - `src/features/connect/useSyncEngine.ts` (the sibling `onChange` subscriber: `unsubscribeRef.current = onChange(...)`, teardown on unmount — mirror this lifecycle)
    - `src/features/search/searchIndex.ts` (plan-02 output — the MiniSearch instance + `fieldTextById` projection + the async document builder; add the pure `applyChange` here)
    - `src/features/search/useSearchIndex.ts` (plan-01/02 output — replace the rebuild-on-change with the incremental subscription)
    - `tests/features/positionCache.test.ts` (vitest style + fake-indexeddb)
  </read_first>
  <action>
    Add a PURE `applyChange(index, fieldTextById, ev, personById, customDefs)` to `searchIndex.ts` that maps a repository `ChangeEvent` to the index (single source of truth for the incremental path): `op:'create'` → build the document for the person and `index.add(doc)` (and set its `fieldTextById` entry); `op:'update'` → `index.replace(doc)` (MiniSearch replace = discard+add; guard the not-yet-indexed case by falling back to `add`); `op:'delete'` → `index.discard(entityId)` and drop its `fieldTextById` entry. Only act on `entityType === 'people'`. Because the document build resolves link-to-entity target names, `applyChange` is async.

    Update `useSearchIndex.ts`: keep the ONE initial build from the people snapshot on mount, then subscribe to `repository.onChange` in a `useEffect` (mirroring `useSyncEngine`: capture the unsubscribe, return it for teardown). For `entityType === 'people'` events, route through `applyChange` (incremental — do NOT rebuild the whole index). For `entityType === 'fieldDefs'` events (a field add/rename/type-change/soft-delete changes the indexed field set), rebuild the index (the field set itself changed — a coarse rebuild is correct here and is NOT the per-entity path criterion 3 targets); a `link-to-entity` target rename arrives as a `people`/`groups` update — re-index the affected/referring person via `applyChange` (planner discretion: a simple, correct choice is to `replace` the changed person; referrers refresh on their own next change). Trigger a React re-render of results after an incremental change so the live view reflects it (e.g. bump a state counter in the onChange handler).

    Create `tests/features/searchIncremental.test.ts` (unit): build an index from an initial people set, then drive `applyChange` directly with create/update/delete events and assert the search results reflect each change WITHOUT calling the full builder again (assert via a spy or by constructing the index once and only invoking `applyChange`).

    Create `e2e/search-incremental.spec.ts` (follow the browse-and-create harness): open Search with a query that matches an existing person, then create a NEW matching person via `window.__rb.createPerson` and assert the new person appears in the results WITHOUT a page reload; also assert a matched-field snippet row renders a `<mark>` (`search-snippet`) and that the search feature source contains no raw-HTML injection.
  </action>
  <behavior>
    - After `buildIndex([Smith])`, `applyChange(index, ftbi, {entityType:'people', entityId:jonesId, op:'create'}, {jonesId: Jones}, defs)` then searching 'jon' returns Jones — added without a rebuild.
    - `applyChange(... op:'update' ...)` with a renamed person replaces the stale document (old name no longer matches; new name does).
    - `applyChange(... op:'delete' ...)` discards the person (no longer returned) and drops its `fieldTextById` entry.
    - A `fieldDefs` create/soft-delete changes the searchable field set (a newly added custom field becomes searchable; a soft-deleted one stops matching).
  </behavior>
  <acceptance_criteria>
    - `npx vitest run tests/features/searchIncremental.test.ts` passes; the create/update/delete cases run through `applyChange` only (the full builder is invoked once at setup, verified by spy or call-count).
    - E2E (`e2e/search-incremental.spec.ts`): a person created via `window.__rb.createPerson` while the Search view is open appears in results with no reload (incremental add); a non-name match row renders `search-snippet` containing a `<mark>`.
    - `useSearchIndex.ts` imports `onChange` from `@/db/repository` and returns its unsubscribe from the effect (no dangling subscription).
    - `grep -rn "dangerouslySetInnerHTML={" src/features/search/` returns no matches.
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/features/searchIncremental.test.ts && npm run typecheck && npm run lint && npm run build:e2e && npx playwright test e2e/search-incremental.spec.ts</automated>
  </verify>
  <done>The index stays fresh through the repository change signal — create/update/delete route through add/replace/discard (not a full rebuild), a field-schema change updates the searchable set, new people appear live in results, and the snippet evidence renders safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matched field value → highlighted DOM | User-authored field text + the matched term are rendered with emphasis in the result row |
| repository change events → in-memory index | Post-commit change events mutate the local search index |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Info-disclosure (XSS) | snippet.ts `<mark>` + SearchResultRow snippet | mitigate | The matched term, snippet fragments, and field label render as real React elements/children; the highlight is a genuine `<mark>` — no raw-HTML injection. HARD acceptance criterion: the injection attribute form is absent from `src/features/search/`, and the snippet test asserts the mark is a React element, not an HTML string. |
| T-05-03 | Tampering | incremental index vs. source of truth | accept | All `onChange` emits are post-commit (repository stamps then emits), so the index only ever reflects persisted state; a missed event degrades to a stale local projection (rebuildable on reload), never corrupts Dexie. Single-curator boundary. |
</threat_model>

<verification>
- `npx vitest run tests/features/snippet.test.ts tests/features/searchIncremental.test.ts` — snippet highlight + incremental add/replace/discard.
- `npx playwright test e2e/search-incremental.spec.ts` — live-appear + snippet `<mark>`.
- `npm run typecheck && npm run lint` — no regressions.
- `grep -rn "dangerouslySetInnerHTML={" src/features/search/` → no matches.
</verification>

<success_criteria>
- D-09 evidence is live: non-name matches show the matched field + highlighted term in a real `<mark>`; name matches use the plain fallback line.
- SRCH-01 criterion 3 is met: the index updates incrementally via the repository change signal (add/replace/discard), not a per-load rebuild; new/edited/deleted people reflect live in results.
- The XSS boundary holds across all rendered user text.
</success_criteria>

<output>
Create `.planning/phases/05-field-scoped-search/05-03-SUMMARY.md` when done.
</output>

---
phase: 05-field-scoped-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/features/search/searchIndex.ts
  - src/features/search/useSearchIndex.ts
  - src/features/search/SearchView.tsx
  - src/features/search/SearchResultRow.tsx
  - src/features/search/SearchView.module.css
  - src/features/nav/ViewSwitcher.tsx
  - src/app/App.tsx
  - tests/features/searchIndex.test.ts
  - e2e/search.spec.ts
autonomous: false
requirements: [SRCH-01]
user_setup: []

must_haves:
  truths:
    - "A 'Search' entry appears in the left-nav ViewSwitcher rail (sibling of People/Graph) with no count pill (D-01)"
    - "Selecting Search swaps the main surface to a paper-chrome Search view with a debounced search input (D-02)"
    - "Typing 'smi' surfaces a person named 'Smith' via prefix+fuzzy matching over built-in fields (D-06, SRCH-01)"
    - "Clicking a result row opens that person's ProfileSidebar; the row's Show-on-map action navigates to their placement (D-10)"
    - "Results begin only at the 2nd typed character; 0-1 chars show the pre-query prompt (D-08, D-11)"
  artifacts:
    - path: "src/features/search/searchIndex.ts"
      provides: "MiniSearch index service — built-in field config, build-from-people, field-restricted search returning ranked hits with match metadata"
      min_lines: 40
    - path: "src/features/search/useSearchIndex.ts"
      provides: "Hook owning the index instance; builds from the live people snapshot and exposes search(query, fields)"
    - path: "src/features/search/SearchView.tsx"
      provides: "Search view — input + windowed results + pre-query/zero-match states + live-region"
    - path: "src/features/search/SearchResultRow.tsx"
      provides: "BrowseRow-variant result row (thumbnail + name + open + show-on-map)"
    - path: "src/features/nav/ViewSwitcher.tsx"
      provides: "'search' ViewKey + VIEW_ITEMS entry + NO_PILL membership + widened EntityViewKey exclude"
      contains: "search"
  key_links:
    - from: "src/features/search/SearchView.tsx"
      to: "src/features/search/useSearchIndex.ts"
      via: "calls search(debouncedQuery, activeFields) from the hook"
      pattern: "useSearchIndex"
    - from: "src/app/App.tsx"
      to: "src/features/search/SearchView.tsx"
      via: "render branch for activeView === 'search' wiring onOpen=openFromList('people'), onShowOnMap=showOnMap"
      pattern: "SearchView"
  prohibitions:
    - "No Dexie migration / schema-push task — this is Dexie (not Drizzle) and Phase 5 is read-only over existing tables"
    - "No Web Worker — MiniSearch runs on the main thread (D-02)"
    - "No matched-field checkbox scope panel yet (that is plan 02); this slice searches all built-in fields"
    - "No raw-HTML injection for any rendered user text — entity names and the echoed {query} render as React children"
---

<objective>
Deliver the Field-Scoped Search spine (SRCH-01): a dedicated "Search" view in the left-nav rail where typing a query surfaces tolerant, fuzzy matches over People's built-in attributes, each result opening the existing ProfileSidebar. This is the thinnest end-to-end slice — index service + query + view + reactivity working together — that proves the search spine before plan 02 layers on the field-scope checkboxes and plan 03 layers on the matched-field snippet and incremental freshness.

Purpose: Prove the signature search feature end-to-end (D-01/D-02/D-06/D-08/D-10) with the one net-new dependency (MiniSearch) wired to the existing reuse surfaces (ViewSwitcher, BrowseRow patterns, ProfileSidebar, Show-on-map).
Output: `minisearch` installed; a MiniSearch index service + hook; a Search view + result row; ViewSwitcher + App wired to the new `'search'` view; a unit test proving prefix+fuzzy+field-restriction; an e2e proving type→match→open.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-field-scoped-search/05-CONTEXT.md
@.planning/phases/05-field-scoped-search/05-PATTERNS.md
@.planning/phases/05-field-scoped-search/05-UI-SPEC.md

# Reuse surfaces (verify current shapes before writing code)
@src/features/nav/ViewSwitcher.tsx
@src/app/App.tsx
@src/features/browse/BrowseList.tsx
@src/features/browse/BrowseRow.tsx
@src/features/browse/browseTypes.ts
@src/domain/types.ts
@src/features/person-map/editor/PersonPicker.module.css
</context>

<artifacts_this_phase_produces>
The following symbols are NET-NEW across Phase 5 (exclude from source-grounding drift checks). This plan (05-01) creates the ones marked ★; plans 02/03 add the rest:

- ★ `src/features/search/searchIndex.ts` — MiniSearch index service (built-in field config, `buildIndex(people)`, `search(query, fields)`; extended for custom fields in plan 02, incremental `applyChange` in plan 03).
- ★ `src/features/search/useSearchIndex.ts` — hook owning the index; exposes `search`. (Custom-field awareness added plan 02; incremental `onChange` wiring added plan 03.)
- ★ `src/features/search/SearchView.tsx` — the Search view composition (input + results + states). (Scope panel + all-fields-off guard added plan 02.)
- ★ `src/features/search/SearchResultRow.tsx` — BrowseRow-variant result row. (Matched-field snippet added plan 03.)
- ★ `src/features/search/SearchView.module.css` — token-only stylesheet.
- ★ New exported types from `searchIndex.ts`: `SearchHit`, the built-in field-key constants, the boost map.
- ★ New `'search'` `ViewKey` literal in `ViewSwitcher.tsx`.
- `src/features/search/ScopePanel.tsx`, `src/features/search/useScopeSelection.ts` (plan 02).
- `src/features/search/snippet.ts` (plan 03).
- New dependency: `minisearch` 7.2.x in `package.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — verify minisearch before install</name>
  <action>STOP for human confirmation before installing the one net-new dependency. Do NOT run `npm install` until the human types "approved". This is a package-legitimacy gate and is never auto-approvable.</action>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists for this phase (the roadmap set a deliberate skip-research flag). Per the fallback policy the one net-new dependency, `minisearch`, is treated as [ASSUMED] and must be human-verified before it is installed. `minisearch` is the prescriptive-stack search library named in `.claude/CLAUDE.md` (MIT, version verified against the npm registry 2026-06-24 in that file's Sources section).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/minisearch and confirm: the package is `minisearch` (author lucaong), license is MIT, latest is in the 7.2.x line, and weekly downloads + repo (github.com/lucaong/minisearch) look healthy/maintained.
    2. Confirm it matches the prescriptive pick in `.claude/CLAUDE.md` (MiniSearch 7.2.x, MIT).
    3. Confirm it is a pure logic library — no postinstall scripts, no network/DOM side effects.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install minisearch@^7.2.0`, or name a concern (typosquat / wrong package / license mismatch) to block.</resume-signal>
  <done>Human has verified minisearch on npmjs.com (name/author/MIT/7.2.x/maintained) and typed "approved"; the install in Task 2 is authorized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install minisearch + build the index service and hook (SRCH-01, D-06)</name>
  <files>package.json, src/features/search/searchIndex.ts, src/features/search/useSearchIndex.ts, tests/features/searchIndex.test.ts</files>
  <read_first>
    - `.claude/CLAUDE.md` (MiniSearch 7.2.x is the prescribed lib; per-field indexing; `fuzzy`+`prefix`+`boost`; free/OSS)
    - `.planning/phases/05-field-scoped-search/05-PATTERNS.md` (§ searchIndex.ts + § useSearchIndex.ts — the MiniSearch config, build-from-Dexie, and the persistence analog `positionCache.ts`)
    - `src/domain/types.ts` (the `Person` shape: name/phone/description/tags/notes; ignore photo/gallery/custom for this slice)
    - `src/features/graph/positionCache.ts` (the derived-rebuildable-projection-over-Dexie analog)
    - `tests/features/positionCache.test.ts` (vitest style: `beforeEach` clearing tables, `describe/it`, `@/` alias, fake-indexeddb via tests/setup.ts)
    - `src/db/schema.ts` (`db.people.toArray()` is the index source of truth; NO schema change)
  </read_first>
  <action>
    Install the approved dependency: `npm install minisearch@^7.2.0` (pins the 7.2.x line; verify `package.json` records it under dependencies).

    Create `src/features/search/searchIndex.ts` exporting:
    - Built-in field-key constants — an ordered list of the five searchable built-ins keyed by STABLE ids: `builtin:name`, `builtin:phone`, `builtin:description`, `builtin:tags`, `builtin:notes`, each with a display label (Name/Phone/Description/Tags/Notes). Export both the key list and a key→label map. These keys are the MiniSearch document field names AND (in plan 02) the scope-checkbox keys — pick them once here so plan 02 supplies a subset unchanged.
    - A boost map (D-07 field-boosted ranking): name highest (3), then tags and phone (2), then description and notes (1). Custom fields are omitted (neutral default weight 1) — they arrive in plan 02.
    - A `buildIndex(people: Person[])` factory that constructs `new MiniSearch({ fields: <the five built-in keys>, idField: 'id', searchOptions: { fuzzy: 0.2, prefix: true, boost: <the boost map> } })` (D-06 moderate typo tolerance + prefix), builds one document per person mapping each built-in key to that person's value (tags joined with a space; undefined built-ins → empty string), and `addAll`s them. Return the MiniSearch instance.
    - A `search(index, query, fields)` wrapper that trims the query, returns `[]` when the trimmed length is below 2 (D-08 two-char threshold) OR when `fields` is empty, and otherwise calls `index.search(query, { fields })` — the `fields` argument is the query-time field restriction (this is the seam plan 02's checkboxes drive). Map each MiniSearch `SearchResult` to an exported `SearchHit` type carrying at least `{ id: string; score: number; match: MatchInfo; terms: string[] }` (keep `match`/`terms` on the hit so plan 03's snippet can read which field matched — do not drop them).

    Create `src/features/search/useSearchIndex.ts`: a hook that owns one index instance. Read the people via `useLiveQuery(() => db.people.toArray())` and rebuild the index in a `useMemo`/effect whenever that snapshot changes (simple + correct for the spine — plan 03 replaces this rebuild with incremental `add`/`replace`/`discard` for criterion 3). Expose `search(query: string, fields: string[]): SearchHit[]` bound to the current index. For this slice, callers pass the full built-in key list as `fields`.

    Do NOT persist the index to the cloud/backup and do NOT add any serializer/SyncEngine/BackupSchema change — the index is a local, rebuildable projection of `db.people`.
  </action>
  <behavior>
    - `buildIndex([Smith, Jones])` then `search(index, 'smi', <all built-in keys>)` returns a hit for Smith (prefix match).
    - A fuzzy query with one transposed/edited character (e.g. 'smtih') still returns Smith within the 0.2 edit-distance tolerance.
    - `search(index, 'smith', ['builtin:name'])` returns Smith; restricting to a field the term is NOT in returns no Smith hit (proves field restriction is the scoping seam).
    - `search(index, 's', <all keys>)` returns `[]` (below the 2-char threshold, D-08).
    - `search(index, 'smith', [])` returns `[]` (no fields selected).
    - A name query outranks a stray notes-only match (boost applied) — a person whose NAME is 'Smith' scores above a person whose NOTES merely contain 'smith'.
  </behavior>
  <acceptance_criteria>
    - `package.json` dependencies include `minisearch` at `^7.2.0` (or the exact 7.2.x resolved version).
    - `searchIndex.ts` constructs MiniSearch with `fuzzy: 0.2` and `prefix: true` and a boost map where the name key's weight is the highest.
    - `npx vitest run tests/features/searchIndex.test.ts` passes and includes the smith-prefix, fuzzy-tolerance, field-restriction, 2-char-threshold, and boost-ordering assertions above.
    - `npm run typecheck` passes (SearchHit exported; MatchInfo typed from minisearch).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/features/searchIndex.test.ts && npm run typecheck</automated>
  </verify>
  <done>The index service and hook exist; a query surfaces prefix+fuzzy People matches over built-in fields with name-boosted ranking, restricted to the supplied field set, gated at 2 chars — all proven by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the Search view into the nav + shell and build the input/results/states UI (D-01/D-02/D-08/D-10/D-11)</name>
  <files>src/features/nav/ViewSwitcher.tsx, src/app/App.tsx, src/features/search/SearchView.tsx, src/features/search/SearchResultRow.tsx, src/features/search/SearchView.module.css, e2e/search.spec.ts</files>
  <read_first>
    - `src/features/nav/ViewSwitcher.tsx` (add `'search'` to the `ViewKey` union, add a `VIEW_ITEMS` entry with the lucide `Search` glyph, add `'search'` to `NO_PILL`; AND widen `type EntityViewKey = Exclude<ViewKey, 'map' | 'graph'>` to also exclude `'search'` — otherwise the `counts` Record demands a 'search' key and typecheck breaks. The roving-focus group is generic; the new item rides it with no wiring change.)
    - `src/app/App.tsx` (add a `activeView === 'search'` render branch beside map/graph/BrowseList; AND widen `type EntityView = Exclude<ViewKey, 'map' | 'graph'>` to exclude `'search'` so the BrowseList fallback never receives it. Reuse `openFromList('people', id)` and `showOnMap(id)` verbatim for the SearchView props.)
    - `src/features/browse/BrowseList.tsx` (copy the windowing block: `ROW_HEIGHT = 64`, `OVERSCAN = 6`, scrollRef + ResizeObserver `viewportH`, the `useMemo` → `{start,end,padTop,padBottom}`, and the `rows.slice(start,end)` inside a padTop/padBottom spacer; copy the loading/empty state-panel structure for the state screens)
    - `src/features/browse/BrowseRow.tsx` (copy the row skeleton: `role="button"` + Enter/Space → onOpen, `useEntityThumb(photo, gallery)` from `@/features/browse/useEntityThumb`, `initialsOf` from `@/features/common/initials`, the round-avatar people branch, name line, and the Show-on-map button branch with `data-testid="browse-show-on-map"` + `stopPropagation`. Omit the openable-map branch — people are never maps. The overflow Edit/Delete menu is optional this phase (B7) — omit it.)
    - `src/features/browse/browseTypes.ts` (`isSpatial('people') === true` gates the enabled Show-on-map action)
    - `src/features/person-map/editor/PersonPicker.module.css` (reuse the `.input` treatment: paper-shade fill, hairline border, amber `:focus-visible` ring)
    - `src/features/browse/BrowseList.module.css` (reuse `.row`/`.thumbRound`/`.rowName`/`.rowSecondary`/`.showOnMap`/`.empty`/`.emptyGlyph`/`.emptyHeading`/`.emptyBody` classes for the row + state panels — extend, do not fork)
    - `.planning/phases/05-field-scoped-search/05-UI-SPEC.md` (§ S1 input, § S3 row, § S4 states, Copywriting Contract — the exact copy strings)
  </read_first>
  <action>
    ViewSwitcher: add `Search` to the lucide import; add `'search'` to the `ViewKey` union; add `{ key: 'search', label: 'Search', icon: Search }` to `VIEW_ITEMS` as a sibling of People/Graph per the UI-SPEC IA (D-01); add `'search'` to the `NO_PILL` set (no count pill, D-01); widen `EntityViewKey` to `Exclude<ViewKey, 'map' | 'graph' | 'search'>`.

    App.tsx: widen `type EntityView = Exclude<ViewKey, 'map' | 'graph' | 'search'>`; add a render branch — when `activeView === 'search'` render `<SearchView onOpen={(id) => openFromList('people', id)} onShowOnMap={(id) => void showOnMap(id)} />` — beside the existing map/graph branches and before the BrowseList fallback.

    Create `src/features/search/SearchResultRow.tsx` — a `BrowseRow` variant with props `{ entity: Person; onOpen: (id: string) => void; onShowOnMap: (id: string) => void }`. Copy the BrowseRow row skeleton (whole-row `role="button"` → `onOpen(entity.id)`, Enter/Space handler, round avatar via `useEntityThumb` + `initialsOf`, name line). Secondary line for this slice: the normal BrowseRow fallback (tags chips via `entityTags(entity,'people')`, else the "updated Nd ago" meta) — the matched-field snippet arrives in plan 03. Include the enabled Show-on-map button (reuse the `data-testid="browse-show-on-map"` wiring + `stopPropagation` + `onShowOnMap`). Render name as a React child only — never build markup from a raw HTML string.

    Create `src/features/search/SearchView.tsx` (D-02 live-as-you-type):
    - A native `<input type="search">` (accessible name "Search people", placeholder "Search people…", leading decorative lucide `Search` glyph, min-height 44px) styled via the reused `.input` treatment.
    - Debounce the query ~200ms before querying; run `useSearchIndex().search(debouncedQuery, <all built-in keys>)` on the main thread (D-02). Map each hit id to the live Person via a `useLiveQuery(() => db.people.toArray())`-backed `Map<id, Person>` so row rendering (thumbnail/name/tags) reflects live data (skip a hit whose person is transiently missing).
    - Windowed results list copied from BrowseList (64px rows) rendering `SearchResultRow` per hit.
    - Two DISTINCT state panels (D-11), each a centered panel with a lucide glyph + Display/28 heading + Body/16 sub-line and NO CTA button (B4): pre-query prompt when the trimmed query is below 2 chars (glyph `Search`, heading "Search people", body "Search people by name, tags, or any field — then narrow it with the checkboxes."), and zero-match when the query is ≥2 chars with 0 hits (glyph `SearchX`, heading `No people match "{query}"` with `{query}` rendered as a React child, body "Check your spelling, or turn more fields on to widen the search."). The all-fields-off guard is a THIRD distinct state that arrives with the checkboxes in plan 02 — leave a clear seam, do not fold it into zero-match.
    - A visually-hidden `aria-live="polite"` region announcing "{n} people match" / "No people match" as results change (mirror the App.tsx Show-on-map live region).

    Create `src/features/search/SearchView.module.css` — token-only (reuse `--paper`/`--paper-shade`/`--ink`/`--ink-muted`/`--amber`/`--hairline`/`--space-*`/`--radius-*`/`--font-*`); the `.input` block mirrors PersonPicker's; reuse BrowseList.module.css row/state classes for the results column rather than duplicating them.

    Add `data-testid="view-search"` rides automatically from the ViewSwitcher item (`view-${key}`). Add `data-testid="search-input"` to the input and reuse `data-testid="browse-row"`/`browse-row-name`/`browse-show-on-map` in the result row.

    Create `e2e/search.spec.ts` following the `e2e/browse-and-create.spec.ts` pattern (reset db, wait for `window.__rb`, suppress privacy notice).
  </action>
  <acceptance_criteria>
    - `ViewSwitcher.tsx` `VIEW_ITEMS` contains an item with `key: 'search'`, `'search'` is a member of `NO_PILL`, and `EntityViewKey` excludes `'search'`.
    - `App.tsx` has a render branch for `activeView === 'search'` that mounts `SearchView` with `onOpen`/`onShowOnMap` wired to `openFromList('people', …)` / `showOnMap`.
    - E2E (`e2e/search.spec.ts`): seed people including one named "Smith" and one named "Jones" via `window.__rb.createPerson`, open the Search view via `view-search`, type "smi" into `search-input`, and assert a `browse-row-name` reading "Smith" is visible while "Jones" is not; clicking the row opens `profile-sidebar` with `profile-name` "Smith"; typing a single char shows the pre-query prompt heading "Search people"; a ≥2-char no-match query shows a heading containing the typed query.
    - `grep -rn "dangerouslySetInnerHTML={" src/features/search/` returns no matches (user text renders as React children).
    - `npm run typecheck` and `npm run lint` pass.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build:e2e && npx playwright test e2e/search.spec.ts</automated>
  </verify>
  <done>A "Search" rail entry opens a paper-chrome Search view; typing ≥2 chars surfaces tolerant People matches; a row click opens the ProfileSidebar and Show-on-map works; pre-query and zero-match states render distinctly with the echoed query as a React child.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| person data → rendered DOM | Untrusted user-authored names + the echoed `{query}` cross into the Search view and result rows |
| npm registry → build | The net-new `minisearch` dependency enters the supply chain |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / Info-disclosure (XSS) | SearchView state copy + SearchResultRow name | mitigate | Entity names and the echoed `{query}` in the zero-match heading render as React children only — no raw-HTML injection. A leaked Drive token is the modeled consequence (T-03-01 lineage). HARD acceptance criterion: the JSX-attribute injection form is absent from `src/features/search/`. |
| T-05-SC | Tampering (supply chain) | `minisearch` npm install | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; pin to the `7.2.x` line; prescribed-stack pick pre-verified in `.claude/CLAUDE.md`. Never auto-approvable. |
| T-05-NS | Info-disclosure (exfiltration surface) | MiniSearch index | accept | The index is a local, rebuildable projection of `db.people`; it never enters the cloud/backup serializer, so it introduces no new sync/exfiltration surface (single-curator, provider-level security v1 — Phase-04 register ACCEPTED). |
</threat_model>

<verification>
- `npx vitest run tests/features/searchIndex.test.ts` — prefix, fuzzy, field-restriction, threshold, boost ordering.
- `npm run typecheck && npm run lint` — no type/lint regressions (ViewKey/EntityViewKey/EntityView widenings compile).
- `npx playwright test e2e/search.spec.ts` — type→match→open→states end-to-end.
- `grep -rn "dangerouslySetInnerHTML={" src/features/search/` → no matches.
</verification>

<success_criteria>
- SRCH-01 spine is live: a dedicated Search view where typing surfaces tolerant, name-boosted fuzzy People matches over built-in attributes.
- The `'search'` view is wired through ViewSwitcher + App with no typecheck fallout.
- Results open the existing ProfileSidebar and Show-on-map; pre-query and zero-match states are distinct; the index is a local rebuildable projection (no cloud/backup change, no schema migration).
</success_criteria>

<output>
Create `.planning/phases/05-field-scoped-search/05-01-SUMMARY.md` when done.
</output>

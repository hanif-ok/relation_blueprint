---
phase: 04-relationships-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.ts
  - src/domain/schemas.ts
  - src/db/schema.ts
  - src/db/repository.ts
  - tests/db/repository.relationships.test.ts
  - tests/backup/roundtrip.relationships.test.ts
autonomous: true
requirements: [REL-01, REL-02]
user_setup: []

must_haves:
  truths:
    - "createRelationshipLink persists a link with fromType/fromId/toType/toId/directed, stamped updatedAt+dirty (D-01/D-04)"
    - "listRelationshipsFor(x) returns every link where x is the from OR the to endpoint (indexed .or())"
    - "A Location-typed endpoint is rejected at the write path AND at the backup import boundary (D-07, V5)"
    - "Deleting a Person or Group cascades (removes) every relationship-link it is an endpoint of (no orphan)"
    - "Endpoint fields (label/date/notes + endpoints) survive export and restore round-trip unchanged (REL-02)"
  artifacts:
    - path: "src/domain/types.ts"
      provides: "RelationshipEndpointType + optional endpoint fields on RelationshipLink"
      contains: "RelationshipEndpointType"
    - path: "src/domain/schemas.ts"
      provides: "RelationshipLinkSchema endpoint fields with z.enum(['people','groups'])"
      contains: "z.enum(['people', 'groups'])"
    - path: "src/db/schema.ts"
      provides: "Dexie version(5) index-only upgrade adding fromId/toId"
      contains: "this.version(5)"
    - path: "src/db/repository.ts"
      provides: "listRelationshipsFor + endpoint fields on create/update + cascade on deleteEntity"
      exports: ["listRelationshipsFor"]
    - path: "tests/db/repository.relationships.test.ts"
      provides: "create/validate/reverse-lookup/cascade unit coverage (REL-01)"
    - path: "tests/backup/roundtrip.relationships.test.ts"
      provides: "endpoints survive export/restore + import rejects Location endpoint (REL-02, V5)"
  key_links:
    - from: "src/db/repository.ts (createRelationshipLink)"
      to: "src/domain/schemas.ts (RelationshipLinkSchema)"
      via: "RelationshipLinkSchema.parse validates endpoint enum at the write path"
      pattern: "RelationshipLinkSchema\\.parse"
    - from: "src/db/repository.ts (listRelationshipsFor)"
      to: "src/db/schema.ts (fromId/toId indexes)"
      via: "db.relationshipLinks.where('fromId').equals(x).or('toId').equals(x)"
      pattern: "where\\('fromId'\\).*or\\('toId'\\)"
    - from: "src/db/repository.ts (deleteEntity)"
      to: "src/db/schema.ts (relationshipLinks table)"
      via: "cascade delete of relationship-links inside the existing rw transaction"
      pattern: "relationshipLinks.*delete"
---

<objective>
Establish the shared relationship data foundation that all three Phase-4 vertical slices project from: extend the existing endpoint-less `RelationshipLink` shell (Phase-2 D-08) with an ordered endpoint pair + `directed` flag, add the reverse-lookup index, cascade relationship-links on entity delete, and prove endpoints round-trip through backup. This is the data spine behind REL-01 (authoring) and REL-02 (per-link data) — the authoring UI (04-02), map connectors (04-03), and graph (04-04) all read from it.

Purpose: Turn the data-bearing shell into real authored relationships (per-link direction, D-01; single canonical link queried from both ends, D-04; people|groups endpoints only, D-07) without any new sync/shard plumbing — the `relationship-links` shard is already wired end-to-end.
Output: Extended type↔zod↔Dexie triple, `listRelationshipsFor`, cascade-on-delete, and two green Wave-0 test files.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-relationships-graph/04-CONTEXT.md
@.planning/phases/04-relationships-graph/04-RESEARCH.md
@.planning/phases/04-relationships-graph/04-PATTERNS.md
@.planning/phases/04-relationships-graph/04-VALIDATION.md

# Source of truth for the change (extend in place — self-analogs):
@src/domain/types.ts
@src/domain/schemas.ts
@src/db/schema.ts
@src/db/repository.ts

# Test analogs to copy structure from:
@tests/db/delete.cascade.test.ts
@tests/db/repository.crud.test.ts
@tests/backup/roundtrip.entities.test.ts
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan (exclude from source-drift verification — they do not exist before this plan):
- Type: `RelationshipEndpointType` (`'people' | 'groups'`) in `src/domain/types.ts`
- New optional fields on `RelationshipLink`: `fromType`, `fromId`, `toType`, `toId`, `directed`
- Zod: `RelationshipEndpointTypeSchema` (`z.enum(['people','groups'])`) + the five endpoint fields on `RelationshipLinkSchema` in `src/domain/schemas.ts`
- Dexie: `version(5)` index-only upgrade adding `fromId`, `toId` to `relationshipLinks` in `src/db/schema.ts`
- Function: `listRelationshipsFor(entityId)` in `src/db/repository.ts`
- Endpoint fields threaded through `CreateRelationshipLinkInput` + `createRelationshipLink` parse body; cascade branch in `deleteEntity`
- Test files: `tests/db/repository.relationships.test.ts`, `tests/backup/roundtrip.relationships.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing Wave-0 unit tests for relationship endpoints, reverse-lookup, cascade, and backup round-trip</name>
  <files>tests/db/repository.relationships.test.ts, tests/backup/roundtrip.relationships.test.ts</files>
  <read_first>
    - tests/db/delete.cascade.test.ts — beforeEach clears all tables incl. db.relationshipLinks; fake-indexeddb setup; import surface from @/db/repository
    - tests/db/repository.crud.test.ts — create/get/list assertion idiom for the repository
    - tests/backup/roundtrip.entities.test.ts — export→import round-trip harness (BackupSchema) and per-entity field assertions
    - .planning/phases/04-relationships-graph/04-VALIDATION.md — the exact `<automated>` commands + behaviors these tests must assert
    - .planning/phases/04-relationships-graph/04-RESEARCH.md § Validation Architecture — REL-01/REL-02 behavior rows
  </read_first>
  <behavior>
    - Test: creating a link with fromType='people'/fromId=A, toType='people'/toId=B persists all endpoint fields + directed.
    - Test: creating person↔group and group↔group links persists (endpoints people|groups only).
    - Test: a link whose fromType or toType is 'maps' (a Location) is REJECTED by the write path (schema enum).
    - Test "reverse lookup": listRelationshipsFor(A) returns links where A is the `from` end AND where A is the `to` end (union), and excludes unrelated links.
    - Test "cascade": deleteEntity('people', A) removes every relationship-link with fromId===A or toId===A; deleteEntity('groups', G) likewise.
    - Round-trip test: a link with endpoints + label/date/notes exported then re-imported reconstructs all fields identically.
    - Round-trip test: importing a backup whose relationship-link has fromType='maps' is rejected by BackupSchema before the write transaction (untrusted-at-rest boundary).
  </behavior>
  <action>Author the two failing test files. In tests/db/repository.relationships.test.ts, mirror the delete.cascade.test.ts fake-indexeddb + beforeEach-clear harness; write cases for endpoint create (person↔person, person↔group, group↔group), Location-endpoint rejection (expect the create to throw a zod error), the "reverse lookup" case (name the test so `-t "reverse lookup"` selects it), and the "cascade" case (name it so `-t "cascade"` selects it) asserting listRelationshipsFor and deleteEntity behavior per D-01/D-04/D-07. In tests/backup/roundtrip.relationships.test.ts, mirror roundtrip.entities.test.ts to assert endpoint+label+date+notes survive export/restore and that a Location-typed endpoint in an imported bundle is rejected by BackupSchema (V5, T-04-02). These reference `listRelationshipsFor` and the endpoint fields which do NOT exist yet, so the suite MUST fail to compile/run now (RED).</action>
  <verify>
    <automated>npx vitest run tests/db/repository.relationships.test.ts tests/backup/roundtrip.relationships.test.ts 2>&1 | tail -20 # expect RED (endpoints/listRelationshipsFor not implemented yet)</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and are picked up by vitest.
    - The run FAILS (compile error on missing `listRelationshipsFor`/endpoint fields, or assertion failure) — proving the tests exercise unbuilt behavior.
    - Test names include the substrings "reverse lookup" and "cascade" so the downstream `-t` selectors resolve.
  </acceptance_criteria>
  <done>Two failing test files exist covering REL-01 (create/validate/reverse-lookup/cascade) and REL-02 (round-trip + import-boundary validation).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the RelationshipLink triple + repository endpoints, reverse-lookup, and cascade</name>
  <files>src/domain/types.ts, src/domain/schemas.ts, src/db/schema.ts, src/db/repository.ts</files>
  <read_first>
    - src/domain/types.ts l.232-299 — RelationshipLink interface (add endpoints after `custom`) and the Marker l.265-293 optional-with-default precedent to mirror
    - src/domain/schemas.ts — RelationshipLinkSchema (l.135-146) + MarkerSchema (l.148-170) optional/enum precedent + the compile-time `satisfies` lock near file bottom (l.245/261/276)
    - src/db/schema.ts l.72-121 — version(2)/version(4) stores() blocks; append version(5). This is DEXIE, not Drizzle — version(n).stores() auto-upgrades in-browser; NO migration-push, NO .upgrade() callback for an index-only change (see project memory schema-gate-dexie-false-positive)
    - src/db/repository.ts l.449-499 (CreateRelationshipLinkInput/createRelationshipLink/updateRelationshipLink) and l.181-215 (deleteEntity rw txn — db.relationshipLinks is ALREADY in the transaction table list at l.184; marker cascade idiom at l.190-191)
    - .planning/phases/04-relationships-graph/04-PATTERNS.md § Pattern Assignments (types/schemas/schema/repository) — the copy-from excerpts
  </read_first>
  <behavior>
    - Endpoint fields are OPTIONAL so pre-Phase-4 shell records and old backups still validate (no data migration).
    - `directed` absent is treated as false at every read site (normalize at read, not schema default-required).
    - listRelationshipsFor(x) is a single indexed `.or()` union over fromId/toId.
    - deleteEntity('people'|'groups', id) drops links where fromId===id or toId===id inside the existing rw transaction.
  </behavior>
  <action>In types.ts add `export type RelationshipEndpointType = 'people' | 'groups';` and append optional `fromType?: RelationshipEndpointType; fromId?: string; toType?: RelationshipEndpointType; toId?: string; directed?: boolean;` to the `RelationshipLink` interface after `custom` (D-01), mirroring the `Marker.kind`/`personId`/`targetMapId` optional precedent so old records need no migration. In schemas.ts add `const RelationshipEndpointTypeSchema = z.enum(['people', 'groups']);` and the five matching optional fields to `RelationshipLinkSchema` (keeping the `satisfies` mirror lock green) — the closed enum is the V5 input-validation control (T-04-02) rejecting a Location endpoint at both the write path and the BackupSchema import gate. In schema.ts append `this.version(5).stores({ relationshipLinks: 'id, name, updatedAt, dirty, fromId, toId' });` after the version(4) block with a comment noting it is index-only (Dexie skips undefined keys, no `.upgrade()` needed). In repository.ts: add the five endpoint fields to `CreateRelationshipLinkInput` and thread them through the `RelationshipLinkSchema.parse({...})` body in `createRelationshipLink` (pass-through exactly like `label`/`date`); `UpdateRelationshipLinkPatch` (`Partial<Omit<...>>`) already covers them — no edit needed there. Add `export async function listRelationshipsFor(entityId: string): Promise<RelationshipLink[]>` returning `db.relationshipLinks.where('fromId').equals(entityId).or('toId').equals(entityId).toArray()`. In `deleteEntity`, after the marker cascade (l.190-191), add: for `entityType === 'people' || entityType === 'groups'`, `await db.relationshipLinks.where('fromId').equals(id).or('toId').equals(id).delete();` inside the SAME rw transaction (Pitfall 3, prevents dangling-edge crash T-04-03). Do NOT add any migration-push/drizzle task — this is Dexie (Pitfall 6).</action>
  <verify>
    <automated>npx vitest run tests/db/repository.relationships.test.ts 2>&1 | tail -20</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/repository.relationships.test.ts` is GREEN: person↔person/person↔group/group↔group create pass; Location endpoint throws; `-t "reverse lookup"` and `-t "cascade"` pass.
    - `tsc --noEmit` clean — the `satisfies` mirror lock in schemas.ts holds (type↔zod parity).
    - `src/db/schema.ts` contains `this.version(5)` adding `fromId`/`toId` and NO `.upgrade()` callback for it.
    - `listRelationshipsFor` uses `.where('fromId').equals(...).or('toId').equals(...)` (indexed union, not a full-table filter).
  </acceptance_criteria>
  <done>The triple carries optional endpoints, `listRelationshipsFor` answers the reverse lookup via the new indexes, and entity deletion cascades relationship-links — repository unit tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm endpoint serialization round-trips through backup + import-boundary validation</name>
  <files>tests/backup/roundtrip.relationships.test.ts, src/sync/serializer.ts</files>
  <read_first>
    - src/sync/serializer.ts l.36/59/82 — the `relationship-links` branch is ALREADY wired (SHARD_NAMES, serializeShards `.map(clean)`, deserializeShards); new optional fields are plain JSON on the record and ride it with zero plumbing (confirm, do not add a shard/branch)
    - src/domain/schemas.ts — BackupSchema (l.219-233) composes RelationshipLinkSchema; the endpoint enum is the import-time gate
    - tests/backup/roundtrip.entities.test.ts — the export→import harness this test mirrors
    - .planning/phases/04-relationships-graph/04-RESEARCH.md § Pattern 4 — grep evidence that relationship-links is wired through every sync branch
  </read_first>
  <action>Run the round-trip test written in Task 1 and confirm GREEN with the Task-2 schema changes. First READ serializer.ts to confirm the `relationship-links` branch already serializes/deserializes the full record (per D-08 the fields are plain JSON) — expected: NO code change to serializer.ts. If (and only if) the test reveals endpoints are dropped, fix the serializer branch to preserve them; otherwise leave it untouched and record "no change needed" in the SUMMARY. Confirm the import-boundary case (a `fromType:'maps'` endpoint in an imported bundle) is rejected by BackupSchema before the write transaction (V5, T-04-02). Then run the full unit suite to confirm no regression to existing serializer/backup tests.</action>
  <verify>
    <automated>npx vitest run tests/backup/roundtrip.relationships.test.ts 2>&1 | tail -20</automated>
    <automated>npx vitest run tests/backup tests/sync 2>&1 | tail -20 # no regression in existing round-trip/serializer coverage</automated>
  </verify>
  <acceptance_criteria>
    - `tests/backup/roundtrip.relationships.test.ts` GREEN: endpoints + label/date/notes reconstruct identically after export→import.
    - The import-boundary case proves BackupSchema rejects a Location-typed endpoint (throws before any DB write).
    - Existing `tests/backup` + `tests/sync` suites remain green (no shard/branch regression).
  </acceptance_criteria>
  <done>Endpoint fields provably survive the cloud backup round-trip and the untrusted-at-rest import gate rejects invalid endpoint types — REL-02 persistence verified with zero new sync plumbing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring UI → repository write | User-supplied endpoint refs + label/date/notes cross into persisted data |
| imported backup file → Dexie (BackupSchema) | Untrusted-at-rest bundle validated before the write transaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering / Elevation | `RelationshipLinkSchema` endpoint fields at write path + `BackupSchema` import | mitigate | `z.enum(['people','groups'])` on `fromType`/`toType` rejects a Location/`maps` endpoint at BOTH the repository write and the import boundary (ASVS V5) |
| T-04-03 | Denial of Service | `deleteEntity('people'\|'groups')` | mitigate | Cascade-delete relationship-links in the existing rw transaction so a deleted entity leaves no dangling edge/connector that crashes a projection |
| T-04-10 | Tampering | Endpoint pointing at a deleted/missing id (e.g. from an imported backup) | accept (defense downstream) | Endpoint ids are validated as strings here; orphan-guard rendering ("(deleted person/group)") is delivered by the projection plans (04-02/03/04) |
</threat_model>

<verification>
- `npx vitest run tests/db/repository.relationships.test.ts tests/backup/roundtrip.relationships.test.ts` fully green.
- `npx tsc --noEmit` clean (type↔zod `satisfies` parity preserved).
- Full unit suite green: `npm test` (re-run with `--no-file-parallelism` if fork-worker startup timeouts appear — environmental, per project memory).
- NO migration-push/drizzle task exists (Dexie index-only version(5) auto-upgrade).
</verification>

<success_criteria>
- Relationships can be created with people|groups endpoints and per-link direction; Location endpoints rejected (REL-01).
- `listRelationshipsFor(x)` returns the union of links touching x via the new indexes (REL-01 reverse lookup).
- Deleting a Person/Group cascades its relationship-links (REL-01 integrity).
- Endpoint + label/date/notes survive export/restore and the import validation gate (REL-02).
</success_criteria>

<output>
Create `.planning/phases/04-relationships-graph/04-01-SUMMARY.md` when done.
</output>

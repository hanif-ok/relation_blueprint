---
phase: 04-relationships-graph
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/profile/relationships.ts
  - src/features/profile/AddRelationshipDialog.tsx
  - src/features/profile/ProfileSidebar.tsx
  - src/features/nav/NewEntityMenu.tsx
  - tests/features/relationships.test.ts
  - e2e/relationships.spec.ts
autonomous: true
requirements: [REL-01, REL-02]
user_setup: []

must_haves:
  truths:
    - "A Person or Group profile shows a 'Relationships' section listing links where that entity is an endpoint (D-03/D-04)"
    - "'+ Add relationship' lets the author pick another person/group, set direction, and fill label/date/notes, writing ONE canonical link (D-01/D-04); the single label carries the relationship phrasing and reciprocal per-direction labels are deferred for v1 (D-02)"
    - "Locations do NOT get a Relationships section and never appear as a pickable endpoint (D-03/D-07)"
    - "The authored link auto-appears on BOTH endpoints' profiles (single source of truth, D-04)"
    - "The standalone '+ New -> Relationship-link' menu item is removed (D-05); the relationship-links browse list stays"
    - "A deleted other-endpoint renders a muted '(deleted person/group)' row instead of crashing"
  artifacts:
    - path: "src/features/profile/relationships.ts"
      provides: "pure helpers: build relationship rows, resolve other endpoint, direction glyph, filter endpoint-less shells"
      min_lines: 25
    - path: "src/features/profile/AddRelationshipDialog.tsx"
      provides: "entity picker + direction control + label/date/notes -> createRelationshipLink"
    - path: "src/features/profile/ProfileSidebar.tsx"
      provides: "Relationships section (Person+Group) with reactive listRelationshipsFor query + orphan guard"
      contains: "listRelationshipsFor"
    - path: "src/features/nav/NewEntityMenu.tsx"
      provides: "relationship-links item removed (D-05)"
    - path: "tests/features/relationships.test.ts"
      provides: "pure helper unit coverage"
    - path: "e2e/relationships.spec.ts"
      provides: "author-from-profile happy path appearing on both endpoints (REL-01/REL-02)"
  key_links:
    - from: "src/features/profile/ProfileSidebar.tsx"
      to: "src/db/repository.ts (listRelationshipsFor)"
      via: "useLiveQuery(() => id ? listRelationshipsFor(id) : ...) gated to people|groups"
      pattern: "useLiveQuery.*listRelationshipsFor"
    - from: "src/features/profile/AddRelationshipDialog.tsx"
      to: "src/db/repository.ts (createRelationshipLink)"
      via: "writes one canonical link with fromType/fromId/toType/toId/directed"
      pattern: "createRelationshipLink"
---

<objective>
Deliver the authoring vertical slice for REL-01/REL-02: a "Relationships" section on Person and Group profiles (sibling to "Appears on") with a "+ Add relationship" flow that picks the other entity, sets per-link direction, and fills label/date/notes — writing one canonical `RelationshipLink` that auto-appears on both endpoints. Reuses the existing `ProfileSidebar` (D-06) and entity-picker patterns; removes the meaningless standalone "+ New -> Relationship-link" menu item (D-05).

Purpose: Let the curator author relationships in an entity's details (REL-01) with the link carrying its own data (REL-02), using existing surfaces rather than inventing new panels.
Output: `relationships.ts` pure helpers, `AddRelationshipDialog.tsx`, the ProfileSidebar Relationships section, the NewEntityMenu removal, and unit + E2E proofs.
</objective>

<execution_context>
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/cartr/git_stuff/relation_blueprint/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-relationships-graph/04-CONTEXT.md
@.planning/phases/04-relationships-graph/04-PATTERNS.md
@.planning/phases/04-relationships-graph/04-UI-SPEC.md
@.planning/phases/04-relationships-graph/04-VALIDATION.md

# Depends on the data foundation from 04-01:
@.planning/phases/04-relationships-graph/04-01-SUMMARY.md

# Self-analog / reuse surfaces:
@src/features/profile/ProfileSidebar.tsx
@src/features/nav/NewEntityMenu.tsx

# Test analogs:
@tests/features/appearsOn.test.ts
@e2e/profile.spec.ts
@e2e/browse-and-create.spec.ts
</context>

<artifacts_this_phase_produces>
New symbols created by THIS plan:
- File `src/features/profile/relationships.ts` — pure helpers (e.g. `buildRelationshipRows`, `resolveOtherEndpoint`, `directionGlyphFor`); normalizes `directed === true`, filters links missing fromId/toId
- File `src/features/profile/AddRelationshipDialog.tsx` — the add-relationship flow component
- New "Relationships" section block inside `ProfileSidebar.tsx` (rendered only for requestedType people|groups)
- Test files `tests/features/relationships.test.ts`, `e2e/relationships.spec.ts`
- Consumes (already exists after 04-01): `listRelationshipsFor`, `createRelationshipLink`, endpoint fields
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing E2E happy-path + failing pure-helper unit test</name>
  <files>e2e/relationships.spec.ts, tests/features/relationships.test.ts</files>
  <read_first>
    - e2e/profile.spec.ts + e2e/browse-and-create.spec.ts — window.__rb bridge seeding, opening a profile, DOM assertions; E2E runs under `--mode e2e` (testbridge-requires-e2e-build-mode)
    - tests/features/appearsOn.test.ts — pure-helper unit idiom (no DOM render)
    - .planning/phases/04-relationships-graph/04-UI-SPEC.md § R1/R2 — row treatment, direction control default Mutual, empty/orphan states, copy strings ("No relationships yet.", "+ Add relationship")
  </read_first>
  <behavior>
    - E2E: seed two people via __rb; open Person A's profile; use "+ Add relationship" to relate A to B (Mutual); assert a Relationships row naming B appears on A's profile AND, when opening B, a row naming A appears (single canonical link, both ends).
    - Unit: buildRelationshipRows given (thisEntityId, links, endpointNameResolver) returns one row per link with the correct other-endpoint name + direction glyph, and DROPS links missing fromId/toId (legacy shells).
    - Unit: direction glyph is `->` when directed and this entity is `from`, `<-` when this entity is `to`, `<->` when not directed.
  </behavior>
  <action>Write e2e/relationships.spec.ts mirroring profile.spec.ts: seed people via the __rb bridge, open a profile, drive the "+ Add relationship" flow, and assert reciprocal appearance on both endpoints per UI-SPEC R1/R2. Write tests/features/relationships.test.ts mirroring appearsOn.test.ts as pure assertions over the not-yet-existing helpers in src/features/profile/relationships.ts. Both reference unbuilt symbols so they MUST fail now (RED).</action>
  <verify>
    <automated>npx vitest run tests/features/relationships.test.ts 2>&1 | tail -15 # expect RED</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist; the unit run FAILS on missing `src/features/profile/relationships.ts` exports.
    - The E2E spec references `--mode e2e` bridge seeding and asserts reciprocal rows on both endpoints.
  </acceptance_criteria>
  <done>Failing happy-path E2E and failing pure-helper unit test define the authoring slice contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: relationships.ts pure helpers + AddRelationshipDialog + remove NewEntityMenu item</name>
  <files>src/features/profile/relationships.ts, src/features/profile/AddRelationshipDialog.tsx, src/features/nav/NewEntityMenu.tsx</files>
  <read_first>
    - src/features/profile/ProfileSidebar.tsx l.55-64 (groupPlacementsByMap pure-helper precedent) — model relationships.ts on it
    - src/features/nav/NewEntityMenu.tsx l.17-22 — the ITEMS array; delete the relationship-links entry (D-05), keep People/Location/Group
    - src/features/person-map/PersonPicker.tsx (Radix Dialog entity-picker shape) — reuse for the endpoint picker; import ArrowRight/ArrowLeftRight from lucide-react (used in ViewSwitcher)
    - src/db/repository.ts (createRelationshipLink signature + CreateRelationshipLinkInput after 04-01 endpoints) — the write path
    - .planning/phases/04-relationships-graph/04-UI-SPEC.md § R2 — 3-step flow, "Mutual" default, "{This} -> {Other}" preview, neutral paper CTA (NOT amber)
  </read_first>
  <behavior>
    - relationships.ts exports pure, unit-tested helpers (per Task 1 behavior): build rows, resolve other endpoint given this entity id, direction glyph selector; normalize `directed === true`; filter links missing fromId/toId.
    - AddRelationshipDialog: pick other entity (People or Groups only — never Locations, D-03/D-07); choose direction (Directed/Mutual, default Mutual, B5); fill label/date/notes; on save call createRelationshipLink with fromType/fromId (this entity), toType/toId (picked entity), directed, and a derived `name` (label or "{fromName} -> {toName}").
    - Writes go through createRelationshipLink (validate->stamp->emit), never straight to Dexie.
  </behavior>
  <action>Create src/features/profile/relationships.ts with the pure helpers modeled on `groupPlacementsByMap`, making tests/features/relationships.test.ts green. Create src/features/profile/AddRelationshipDialog.tsx as a Radix Dialog (PersonPicker shape) implementing the three-step flow from UI-SPEC R2 — endpoint picker scoped to people|groups (Locations never listed, D-03/D-07), a two-option direction control defaulting to Mutual (B5) with a "{This} -> {Other}" preview when Directed, and label/date/notes inputs (REL-02). On submit call `createRelationshipLink` (D-01/D-04) with the current entity as the `from` endpoint and the picked entity as `to`, passing `directed` and a derived `name`. The "+ Add relationship" trigger is a NEUTRAL paper button (B3 — amber stays reserved for selection). Render all user text (label preview) as JSX children only. In NewEntityMenu.tsx delete the `{ type: 'relationship-links', label: '+ Relationship-link' }` ITEM (D-05); keep People/Location/Group and leave the browse-list edit surface untouched.</action>
  <verify>
    <automated>npx vitest run tests/features/relationships.test.ts 2>&1 | tail -15</automated>
    <automated>npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/features/relationships.test.ts` GREEN (row building, glyph selection, endpoint-less filtering).
    - `NewEntityMenu.tsx` no longer contains a `relationship-links` ITEM; People/Location/Group remain.
    - `AddRelationshipDialog.tsx` calls `createRelationshipLink` with endpoint fields and never lists a Location as a candidate endpoint.
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Pure helpers are green, the add-relationship flow writes a canonical directed/mutual link to the repository, and the meaningless standalone menu item is gone.</done>
</task>

<task type="auto">
  <name>Task 3: ProfileSidebar Relationships section (Person+Group) with reactive read, states, and orphan guard</name>
  <files>src/features/profile/ProfileSidebar.tsx</files>
  <read_first>
    - src/features/profile/ProfileSidebar.tsx l.196-215 ("Appears on" reactive useLiveQuery) and l.326-369 (the rendered list + "(deleted map)" muted-row guard) — the exact structural template to mirror
    - src/features/profile/ProfileSidebar.tsx props l.96-97 (`onOpenEntity` for entity->entity navigation) and the header XSS note l.16 (render user text as React children)
    - src/features/profile/relationships.ts (from Task 2) — the pure helpers to feed the section
    - .planning/phases/04-relationships-graph/04-UI-SPEC.md § R1/R3 + Copywriting Contract — eyebrow "Relationships", empty "No relationships yet.", "(deleted person)"/"(deleted group)"
  </read_first>
  <behavior>
    - Section renders ONLY when requestedType is 'people' or 'groups' (Locations get no section, D-03).
    - Reactively queries `listRelationshipsFor(id)` via useLiveQuery keyed on [id, requestedType]; rebuilds rows from the pure helpers.
    - Each row: direction glyph + label + other-endpoint name; clicking the row opens the relationship record profile (D-06); clicking the endpoint name opens that endpoint via `onOpenEntity` (nested button, its own hit area).
    - Empty state: "No relationships yet." + the "+ Add relationship" button.
    - Orphan guard: an other-endpoint that no longer exists renders a muted "(deleted person/group)" row (T-04-10), mirroring the "(deleted map)" pattern — never a white-screen.
  </behavior>
  <action>Add a "Relationships" section to ProfileSidebar, built like the "Appears on" block: an uppercase Label eyebrow, a `useLiveQuery(() => id ? listRelationshipsFor(id) : Promise.resolve([]), [id, requestedType], [])` gated to `requestedType === 'people' || requestedType === 'groups'` (D-03/D-04), rows assembled by the Task-2 helpers, and the "+ Add relationship" trigger wired to AddRelationshipDialog. Reuse the "(deleted map)" muted-row idiom for the "(deleted person/group)" orphan guard (Pitfall 3, T-04-10) and `onOpenEntity` for endpoint navigation. Render label/notes/names as React children only (XSS boundary T-04-01 — never inject HTML). The existing relationship-record profile already renders label/date/notes (REL-02) — no change there.</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -15</automated>
    <automated>npm run build:e2e >/dev/null 2>&1 && npx playwright test e2e/relationships.spec.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/relationships.spec.ts` GREEN: authoring from a profile creates a link that appears on BOTH endpoints.
    - ProfileSidebar shows the Relationships section for People and Groups and NOT for Locations (verified by reading the render gate + the E2E).
    - Empty state shows "No relationships yet."; a deleted other-endpoint shows a muted "(deleted person/group)" row (no crash).
    - Relationship label/notes render as JSX children (no HTML injection) — verified by reading the section render.
  </acceptance_criteria>
  <done>Person and Group profiles author and display relationships reactively, both endpoints stay in sync from one canonical record, and orphaned endpoints degrade gracefully — REL-01/REL-02 authoring is usable end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author input (label/notes/names) → DOM render | User-authored strings rendered in the profile Relationships section |
| picker selection → repository write | Chosen endpoint type must stay within people|groups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Info-disclosure (XSS→token exfil) | Relationship `label`/`notes` + endpoint names in ProfileSidebar | mitigate | Render exclusively as React children (JSX text) — never `dangerouslySetInnerHTML`; no HTML string injection anywhere in the section |
| T-04-02 | Elevation | AddRelationshipDialog endpoint picker | mitigate | Picker lists only People/Groups (never Locations); the write still validates through `RelationshipLinkSchema` `z.enum(['people','groups'])` (defense in depth with 04-01) |
| T-04-10 | Denial of Service (crash) | Relationships section with a deleted other-endpoint | mitigate | Orphan-guard muted "(deleted person/group)" row instead of dereferencing a missing entity |
</threat_model>

<verification>
- `npx vitest run tests/features/relationships.test.ts` green.
- `npx playwright test e2e/relationships.spec.ts` green (built with `--mode e2e` for the __rb bridge).
- `npx tsc --noEmit` clean.
- Manual/UAT: visual quality of the Relationships section and add-flow (direction preview, Mutual default) confirmed at `/gsd-verify-work`.
</verification>

<success_criteria>
- User can define person↔person, person↔group, and group↔group relationships from a Person/Group profile (REL-01).
- The link carries its own label/date/notes captured in the add-flow (REL-02).
- The relationship appears on both endpoints from one canonical record (D-04); Locations are excluded; the standalone menu item is gone (D-05).
</success_criteria>

<output>
Create `.planning/phases/04-relationships-graph/04-02-SUMMARY.md` when done.
</output>

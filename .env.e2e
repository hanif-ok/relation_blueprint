# E2E-only build mode (`vite build --mode e2e`). Enables the window.__rb test bridge so
# Playwright specs can seed/inspect the real Dexie DB. NEVER loaded by the default
# production build — the bridge exposes mutable DB write APIs and must not ship (WR-01).
VITE_E2E=true
